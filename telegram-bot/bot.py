import asyncio
import atexit
import base64
import hashlib
import io
import logging
import os
import random
import re
import signal
import time
from datetime import datetime, timezone

# ── Защита от дублирования: только 1 экземпляр бота ──────────────
import subprocess as _sp

_PID_FILE = os.path.join(os.path.dirname(os.path.abspath(__file__)), "bot.pid")

def _remove_pid_file() -> None:
    try:
        if os.path.exists(_PID_FILE):
            os.remove(_PID_FILE)
    except OSError:
        pass

def _kill_old_instance() -> None:
    my_pid = os.getpid()
    # 1) По PID-файлу
    if os.path.exists(_PID_FILE):
        try:
            old_pid = int(open(_PID_FILE).read().strip())
            if old_pid != my_pid:
                try:
                    os.kill(old_pid, signal.SIGKILL)
                except (ProcessLookupError, PermissionError, OSError):
                    pass
        except (ValueError, OSError):
            pass
        _remove_pid_file()
    # 2) По имени процесса — убить все «python … bot.py», кроме себя
    for pattern in ["python.*bot\\.py", "python3.*bot\\.py", "bot\\.py"]:
        try:
            out = _sp.check_output(["pgrep", "-f", pattern], text=True)
            for line in out.strip().splitlines():
                pid = int(line.strip())
                if pid != my_pid:
                    try:
                        os.kill(pid, signal.SIGKILL)
                    except (ProcessLookupError, PermissionError, OSError):
                        pass
            time.sleep(0.5)
        except (FileNotFoundError, _sp.CalledProcessError):
            pass
    with open(_PID_FILE, "w") as f:
        f.write(str(my_pid))
    atexit.register(_remove_pid_file)

_kill_old_instance()
from typing import Optional, Dict, List, Any, Tuple
from dotenv import load_dotenv
from telegram import Update, ReplyKeyboardMarkup, ReplyKeyboardRemove, KeyboardButton, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Application, CommandHandler, MessageHandler, CallbackQueryHandler, filters, ContextTypes
from openai import OpenAI

# Буфер фото по user_id для разового разбора (доступен из job)
_pending: Dict[int, Dict[str, Any]] = {}  # user_id -> {"chat_id": int, "file_ids": [(file_id, mime), ...]}
# Последнее заключение по user_id для кнопок «Диагноз» и «Лечение»
_user_last: Dict[int, Dict[str, str]] = {}  # user_id -> {"diagnosis": str, "treatment": str}
# Отложенные задачи батча, когда job_queue недоступен: user_id -> asyncio.Task
_pending_tasks: Dict[int, asyncio.Task] = {}

# Callback data для выбора ИИ
CB_GROQ = "ai:groq"
CB_OPENAI = "ai:openai"
# Поток после приветствия: Начать -> согласие -> опросник
CB_FLOW_START = "flow:start"
CB_CONSENT_ACCEPT = "consent:accept"
CB_CONSENT_DECLINE = "consent:decline"
# После опросника: опрос или загрузить документы
CB_NEXT_SURVEY = "next:survey"
CB_NEXT_UPLOAD = "next:upload"
# Опрос: кнопка «Отправить ответ»
CB_SURVEY_SEND = "survey:send"
# Поток после опроса: запрос → документы → анализ
CB_SEND_DOCS = "docs:send"
CB_NO_DOCS = "docs:none"
CB_SHOW_RESULTS = "results:show"
CB_CONTINUE_YES = "continue:yes"
CB_CONTINUE_NO = "continue:no"
# Авторизация / регистрация
CB_AUTH_LOGIN = "auth:login"
CB_AUTH_REGISTER = "auth:register"
CB_FORGOT_PASSWORD = "auth:forgot"
CB_CLARIFY_ANSWER = "clarify:"

USERS_SHEET_HEADER = ["id", "email", "password", "password_hash", "telegram_id", "telegram_username", "confirmed", "created_at"]

# Единый блок юридического согласия (РФ) — одно сообщение, кнопка «Согласен и продолжить»
CONSENT_TEXT = """📄 <b>Единый блок юридического согласия (РФ)</b>
Информированное согласие пользователя

Нажимая кнопку «Согласен и продолжить», я подтверждаю, что:

• Настоящий сервис не является медицинской организацией, не осуществляет медицинскую деятельность и не оказывает медицинские услуги в смысле Федерального закона № 323-ФЗ «Об основах охраны здоровья граждан в Российской Федерации».

• Бот не устанавливает диагноз, не назначает лечение и не заменяет консультацию врача. Предоставляемая информация носит справочный, информационно-аналитический характер и не является медицинским заключением.

• Я осознаю необходимость обращения к врачу или в медицинскую организацию при ухудшении состояния здоровья.

• В случае возникновения экстренных симптомов (угроза жизни, выраженный болевой синдром, потеря сознания, признаки инсульта или инфаркта и др.) я обязан(а) немедленно обратиться за медицинской помощью или вызвать скорую помощь.

• Я добровольно даю согласие на обработку моих персональных данных, включая специальные категории персональных данных (сведения о состоянии здоровья), в соответствии с Федеральным законом № 152-ФЗ «О персональных данных», исключительно в целях анализа состояния здоровья и формирования информационных рекомендаций в рамках работы сервиса.

• Я подтверждаю, что предоставляю достоверную информацию о своём состоянии здоровья и понимаю, что ответственность за принятие решений о лечении лежит на мне и/или моем лечащем враче."""

# Медицинский опросник — полный список (46 вопросов), пока для теста используем первые 5
MEDICAL_QUESTIONS_FULL = [
    ("Фамилия, имя, отчество", None),
    ("Год рождения (например, 1955)", None),
    ("Пол", "мужской / женский"),
    ("Рост (см)", None),
    ("Вес (кг)", None),
    ("Социальный статус", "проживает один / с семьёй / в учреждении ухода"),
    ("Основная жалоба", "боль / слабость / одышка / повышение температуры / падение / другое"),
    ("Локализация боли", "грудная клетка / живот / голова / поясница / суставы / иное"),
    ("Характер боли", "острая / ноющая / давящая / колющая / приступообразная"),
    ("Интенсивность боли", "слабая / умеренная / выраженная / нестерпимая"),
    ("Длительность симптомов", "сегодня / 1–3 дня / более недели"),
    ("Динамика состояния", "улучшение / без изменений / ухудшение"),
    ("Связано ли ухудшение с физической нагрузкой?", "да / нет"),
    ("Предшествовал ли стресс?", "да / нет"),
    ("Были ли подобные эпизоды ранее?", "да / нет"),
    ("Вызывалась ли скорая медицинская помощь?", "да / нет"),
    ("Артериальная гипертензия?", "да / нет"),
    ("Ишемическая болезнь сердца?", "да / нет"),
    ("Инфаркт миокарда в анамнезе?", "да / нет"),
    ("Инсульт в анамнезе?", "да / нет"),
    ("Сахарный диабет?", "да / нет"),
    ("Хроническая болезнь почек?", "да / нет"),
    ("ХОБЛ / бронхиальная астма?", "да / нет"),
    ("Онкологические заболевания?", "да / нет"),
    ("Принимаете ли вы постоянную терапию?", "да / нет"),
    ("Гипотензивные препараты?", "да / нет"),
    ("Антикоагулянты / антиагреганты?", "да / нет"),
    ("Инсулин / сахароснижающие препараты?", "да / нет"),
    ("Приняты ли препараты сегодня?", "да / нет / пропустил"),
    ("Артериальное давление", "<100 / 100–130 / 130–150 / 150–180 / >180"),
    ("Температура тела", "норма / до 37.5 / 37.5–38.5 / >38.5"),
    ("Частота пульса", "<60 / 60–90 / >90 / не измерял"),
    ("Наличие отёков?", "да / нет"),
    ("Нарушение речи?", "да / нет"),
    ("Онемение конечностей?", "да / нет"),
    ("Нарушение зрения?", "да / нет"),
    ("Судорожный синдром?", "да / нет"),
    ("Потеря сознания?", "да / нет"),
    ("Факт падения?", "да / нет"),
    ("Удар головой?", "да / нет"),
    ("Потеря сознания при падении?", "да / нет"),
    ("Боль в области таза или шейки бедра?", "да / нет"),
    ("Наличие лекарственной аллергии?", "да / нет / не знаю"),
    ("Аллергия на продукты питания?", "да / нет"),
    ("Имеются ли результаты лабораторных анализов?", "да / нет"),
    ("Имеются ли заключения врачей?", "да / нет"),
    ("Имеются ли результаты инструментальных исследований (УЗИ / КТ / МРТ)?", "да / нет"),
    ("Общее самочувствие", "удовлетворительное / средней тяжести / тяжёлое / крайне тяжёлое"),
]
# Сейчас активны только первые 5 вопросов (для теста). Чтобы вернуть все 46: MEDICAL_QUESTIONS = MEDICAL_QUESTIONS_FULL
MEDICAL_QUESTIONS = MEDICAL_QUESTIONS_FULL[:5]


def _ai_choice_keyboard() -> Optional[InlineKeyboardMarkup]:
    """Инлайн-кнопки выбора ИИ (только доступные провайдеры)."""
    buttons = []
    if _use_groq():
        buttons.append(InlineKeyboardButton("Groq", callback_data=CB_GROQ))
    if get_openai_client():
        buttons.append(InlineKeyboardButton("OpenAI (GPT)", callback_data=CB_OPENAI))
    if not buttons:
        return None
    return InlineKeyboardMarkup([buttons])


# Убираем reply-клавиатуру — вместо неё используем ReplyKeyboardRemove
MAIN_KEYBOARD = ReplyKeyboardRemove()
SURVEY_KEYBOARD = ReplyKeyboardRemove()

load_dotenv()

logging.basicConfig(
    format="%(asctime)s - %(name)s - %(levelname)s - %(message)s",
    level=logging.INFO,
)
logger = logging.getLogger(__name__)

# --- Google Таблица ---
# Короткие названия столбцов для каждого вопроса (порядок = MEDICAL_QUESTIONS_FULL)
_Q_LABELS = [
    "фио", "год рождения", "пол", "рост", "вес",
    "соц. статус", "жалоба", "локализация боли", "характер боли", "интенсивность",
    "длительность", "динамика", "нагрузка", "стресс", "ранее",
    "скорая", "гипертензия", "ИБС", "инфаркт", "инсульт",
    "диабет", "ХБП", "ХОБЛ/астма", "онкология", "терапия",
    "гипотензивные", "антикоагулянты", "инсулин", "приём лекарств", "АД",
    "температура", "пульс", "отёки", "речь", "онемение",
    "зрение", "судороги", "потеря сознания", "падение", "удар головой",
    "потеря сознания (падение)", "таз/шейка бедра", "аллергия лек.", "аллергия пищ.", "анализы",
    "заключения", "УЗИ/КТ/МРТ", "самочувствие",
]
# Заголовок: id | дата | <названия вопросов из активного списка>
SHEET_HEADER = ["id", "дата", "telegram", "телефон"] + _Q_LABELS[:len(MEDICAL_QUESTIONS)]


_sheet_cache: Dict[str, Any] = {"wks": None, "ts": 0.0, "header_ok": False}
_SHEET_CACHE_TTL = 300  # 5 минут

def _get_sheet_wks():
    """Возвращает (worksheet, None) или (None, ошибка). Кешируется на 5 минут."""
    import time as _time
    now = _time.time()
    if _sheet_cache["wks"] is not None and (now - _sheet_cache["ts"]) < _SHEET_CACHE_TTL:
        return _sheet_cache["wks"], None

    sheet_id = os.getenv("GOOGLE_SHEET_ID")
    creds_path = os.getenv("GOOGLE_CREDENTIALS_JSON", "credentials.json")
    if not sheet_id or not sheet_id.strip():
        return None, None
    creds_path = (creds_path or "").strip()
    if not creds_path or not os.path.isfile(creds_path):
        logger.warning("Google credentials file not found: %s", creds_path)
        return None, "Файл учётных данных не найден."
    try:
        import gspread
        from google.oauth2.service_account import Credentials
    except ImportError:
        return None, "Установите: pip install gspread google-auth"
    scopes = ["https://www.googleapis.com/auth/spreadsheets", "https://www.googleapis.com/auth/drive"]
    creds = Credentials.from_service_account_file(creds_path, scopes=scopes)
    gc = gspread.authorize(creds)
    sh = gc.open_by_key(sheet_id.strip())
    wks = sh.sheet1
    _sheet_cache["wks"] = wks
    _sheet_cache["ts"] = now
    _sheet_cache["header_ok"] = False
    return wks, None


def _sheet_start_row(tg_username: str = "", tg_phone: str = "") -> Tuple[Optional[int], Optional[int], Optional[str]]:
    """
    Создаёт новую строку (id + дата + telegram + телефон) в начале опроса.
    Возвращает (row_index_1based, id, None) или (None, None, ошибка).
    """
    try:
        wks, err = _get_sheet_wks()
        if wks is None:
            return None, None, err
        rows = wks.get_all_values()

        expected_header = SHEET_HEADER
        if not rows:
            wks.append_row(expected_header, value_input_option="RAW")
            rows = [expected_header]
        elif not _sheet_cache.get("header_ok"):
            current_header = rows[0][:len(expected_header)]
            if current_header != expected_header:
                cell_range = f"A1:{chr(64 + len(expected_header))}1"
                wks.update(cell_range, [expected_header], value_input_option="RAW")
            _sheet_cache["header_ok"] = True

        new_id = 1
        for i, row in enumerate(rows):
            if i == 0:
                continue
            if row and row[0]:
                try:
                    existing_id = int(row[0])
                    if existing_id >= new_id:
                        new_id = existing_id + 1
                except (ValueError, TypeError):
                    pass

        now = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S UTC")
        new_row = [str(new_id), now, tg_username or "", tg_phone or ""] + [""] * len(MEDICAL_QUESTIONS)
        wks.append_row(new_row, value_input_option="RAW")
        row_index = len(rows) + 1
        return row_index, new_id, None
    except Exception as e:
        _sheet_cache["wks"] = None
        logger.exception("Google Sheet start row error: %s", e)
        return None, None, str(e)[:200]


def _sheet_update_answer(row_index: int, step: int, value: str) -> Optional[str]:
    """
    Записывает ответ на вопрос step (1-based) в строку row_index.
    Столбец: A=id, B=дата, C=telegram, D=телефон, E=q1(step1), F=q2(step2), …
    """
    try:
        wks, err = _get_sheet_wks()
        if wks is None:
            return err
        col = step + 4  # step 1 → col 5 (E), step 2 → col 6 (F), ...
        cell_value = (value or "")[:500]
        wks.update_cell(row_index, col, cell_value)
        return None
    except Exception as e:
        _sheet_cache["wks"] = None
        logger.exception("Google Sheet update cell error: %s", e)
        return str(e)[:200]


def _sheet_load_survey_by_tg(tg_username: str) -> Optional[Dict[str, str]]:
    """
    Ищет последнюю строку в таблице опроса по telegram-username.
    Возвращает dict {"q1": ..., "q2": ..., ...} или None.
    """
    if not tg_username:
        return None
    try:
        wks, err = _get_sheet_wks()
        if wks is None:
            return None
        rows = wks.get_all_values()
        if len(rows) <= 1:
            return None
        tg_col = 2  # C (0-based), column "telegram"
        best_row = None
        for row in rows[1:]:
            if len(row) > tg_col and row[tg_col].strip().lower() == tg_username.strip().lower():
                best_row = row
        if not best_row:
            return None
        answers: Dict[str, str] = {}
        n_questions = len(MEDICAL_QUESTIONS)
        for i in range(n_questions):
            col_idx = 4 + i  # E=4, F=5, ...
            val = best_row[col_idx].strip() if col_idx < len(best_row) else ""
            if val:
                answers[f"q{i+1}"] = val
        return answers if answers else None
    except Exception as e:
        logger.warning("_sheet_load_survey_by_tg error: %s", e)
        return None


# --------------- Лист users (авторизация / регистрация) ---------------

_users_cache: Dict[str, Any] = {"wks": None, "ts": 0.0}

def _get_users_wks():
    """Возвращает (worksheet 'users', None) или (None, ошибка). Кешируется."""
    import time as _time
    now = _time.time()
    if _users_cache["wks"] is not None and (now - _users_cache["ts"]) < _SHEET_CACHE_TTL:
        return _users_cache["wks"], None

    sheet_id = os.getenv("GOOGLE_SHEET_ID")
    creds_path = os.getenv("GOOGLE_CREDENTIALS_JSON", "credentials.json")
    if not sheet_id or not sheet_id.strip():
        return None, None
    creds_path = (creds_path or "").strip()
    if not creds_path or not os.path.isfile(creds_path):
        return None, "Файл учётных данных не найден."
    try:
        import gspread
        from google.oauth2.service_account import Credentials
    except ImportError:
        return None, "Установите: pip install gspread google-auth"
    scopes = ["https://www.googleapis.com/auth/spreadsheets", "https://www.googleapis.com/auth/drive"]
    creds = Credentials.from_service_account_file(creds_path, scopes=scopes)
    gc = gspread.authorize(creds)
    sh = gc.open_by_key(sheet_id.strip())
    try:
        wks = sh.worksheet("users")
    except Exception:
        wks = sh.add_worksheet(title="users", rows=1000, cols=len(USERS_SHEET_HEADER))
        wks.append_row(USERS_SHEET_HEADER, value_input_option="RAW")
    try:
        wks.freeze(rows=1)
    except Exception:
        pass
    first_row = wks.row_values(1)
    if first_row != USERS_SHEET_HEADER:
        wks.update("A1:H1", [USERS_SHEET_HEADER], value_input_option="RAW")
        wks.freeze(rows=1)
    _users_cache["wks"] = wks
    _users_cache["ts"] = now
    return wks, None


def _hash_password(password: str) -> str:
    return hashlib.sha256(password.encode("utf-8")).hexdigest()


def _find_user_by_email(email: str) -> Optional[Dict[str, str]]:
    """Ищет пользователя по email. Возвращает dict или None."""
    try:
        wks, err = _get_users_wks()
        if wks is None:
            return None
        rows = wks.get_all_values()
        email_lower = email.strip().lower()
        for i, row in enumerate(rows):
            if i == 0:
                continue
            if len(row) > 1 and row[1].strip().lower() == email_lower:
                return {
                    "row_index": i + 1,
                    "id": row[0] if len(row) > 0 else "",
                    "email": row[1] if len(row) > 1 else "",
                    "password": row[2] if len(row) > 2 else "",
                    "password_hash": row[3] if len(row) > 3 else "",
                    "telegram_id": row[4] if len(row) > 4 else "",
                    "telegram_username": row[5] if len(row) > 5 else "",
                    "confirmed": row[6] if len(row) > 6 else "",
                    "created_at": row[7] if len(row) > 7 else "",
                }
        return None
    except Exception as e:
        _users_cache["wks"] = None
        logger.exception("find_user_by_email error: %s", e)
        return None


def _next_user_id(wks) -> int:
    """Вычисляет следующий ID пользователя (max существующих + 1)."""
    try:
        rows = wks.get_all_values()
        max_id = 0
        for i, row in enumerate(rows):
            if i == 0 or not row:
                continue
            try:
                max_id = max(max_id, int(row[0]))
            except (ValueError, IndexError):
                pass
        return max_id + 1
    except Exception:
        return 1


def _create_user(email: str, password: str, tg_id: int, tg_username: str) -> Optional[str]:
    """Создаёт пользователя (confirmed=no). Возвращает None при успехе, иначе текст ошибки."""
    try:
        wks, err = _get_users_wks()
        if wks is None:
            return err or "Таблица недоступна"
        existing = _find_user_by_email(email)
        if existing:
            return "Пользователь с таким email уже зарегистрирован."
        new_id = _next_user_id(wks)
        now = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S UTC")
        row = [str(new_id), email.strip().lower(), password, _hash_password(password), str(tg_id), tg_username, "no", now]
        wks.append_row(row, value_input_option="RAW")
        return None
    except Exception as e:
        _users_cache["wks"] = None
        logger.exception("create_user error: %s", e)
        return str(e)[:200]


def _confirm_user(email: str) -> bool:
    """Ставит confirmed=yes. Возвращает True при успехе."""
    try:
        wks, err = _get_users_wks()
        if wks is None:
            return False
        rows = wks.get_all_values()
        email_lower = email.strip().lower()
        for i, row in enumerate(rows):
            if i == 0:
                continue
            if len(row) > 1 and row[1].strip().lower() == email_lower:
                wks.update_cell(i + 1, 7, "yes")
                return True
        return False
    except Exception as e:
        _users_cache["wks"] = None
        logger.exception("confirm_user error: %s", e)
        return False


def _check_password(email: str, password: str) -> Optional[Dict[str, str]]:
    """Проверяет пароль. Возвращает данные пользователя или None."""
    user = _find_user_by_email(email)
    if not user:
        return None
    if user.get("confirmed", "").lower() != "yes":
        return None
    if user.get("password_hash") == _hash_password(password):
        return user
    return None


def _update_user_tg(email: str, tg_id: int, tg_username: str) -> None:
    """Обновляет telegram_id и username при авторизации."""
    try:
        wks, err = _get_users_wks()
        if wks is None:
            return
        rows = wks.get_all_values()
        email_lower = email.strip().lower()
        for i, row in enumerate(rows):
            if i == 0:
                continue
            if len(row) > 1 and row[1].strip().lower() == email_lower:
                wks.update_cell(i + 1, 5, str(tg_id))
                wks.update_cell(i + 1, 6, tg_username)
                return
    except Exception as e:
        _users_cache["wks"] = None
        logger.exception("update_user_tg error: %s", e)


def _reset_user_password(email: str) -> Optional[str]:
    """Сбрасывает пароль: генерирует новый, записывает хеш в таблицу. Возвращает новый пароль или None."""
    try:
        wks, err = _get_users_wks()
        if wks is None:
            return None
        rows = wks.get_all_values()
        email_lower = email.strip().lower()
        for i, row in enumerate(rows):
            if i == 0:
                continue
            if len(row) > 1 and row[1].strip().lower() == email_lower:
                new_password = str(random.randint(100000, 999999))
                wks.update_cell(i + 1, 3, new_password)
                wks.update_cell(i + 1, 4, _hash_password(new_password))
                return new_password
        return None
    except Exception as e:
        _users_cache["wks"] = None
        logger.exception("reset_password error: %s", e)
        return None


# Промпт для одного документа
MEDICAL_PROMPT = """Ты помогаешь пожилым людям понять медицинские документы: анализы, заключения врачей, выписки из больницы.

Твоя задача:
1. Прочитай и разбери всё, что видишь на изображении (текст, цифры, печати).
2. Объясни результат ПРОСТЫМИ словами, без сложных медицинских терминов (или сразу поясняй их).
3. Скажи, что в норме, а на что стоит обратить внимание.
4. Если есть отклонения — объясни, что они могут значить и нужно ли срочно к врачу.
5. В конце кратко резюмируй: всё ли в порядке и что делать дальше.

Пиши по-русски, короткими предложениями, доброжелательно. Не пугай, но и не скрывай важное."""

# Промпт для нескольких документов: один общий разбор, хронология, выводы, рекомендации
MULTI_DOC_PROMPT = """Ты помогаешь пожилым людям разобраться в нескольких медицинских документах сразу: анализы, заключения, выписки.

По всем изображениям вместе сделай ОДНО короткое заключение. Уложи в ДВА структурированных абзаца (нумеруй пункты 1-2-3). Пиши максимально просто и чётко.

АБЗАЦ 1 — ЧТО ПРОИЗОШЛО И ЧТО ЭТО ЗНАЧИТ:
1) Кратко: что произошло со здоровьем (хронология, связки между документами).
2) Какие цифры и результаты анализов/исследований это подтверждают (ключевые показатели).
3) Что предполагают врачи, что уже сделали и что планируют сделать.

АБЗАЦ 2 — ЧТО ДЕЛАТЬ И К ЧЕМУ БЫТЬ ГОТОВЫМ:
1) Чего врачи не сделали или не учли — что важно спросить на приёме.
2) Как себя вести и к чему быть готовым (реалистично, без паники).
3) Что точно делать и чего точно не делать (конкретные рекомендации).

Пиши по-русски, простыми словами. Не пугай, но не скрывай важное. Без лишних деталей — только суть и выводы."""

TEXT_PROMPT = """Ты помогаешь пожилым людям разобраться в вопросах здоровья и медицинских терминах. Отвечай простым русским языком, коротко и по делу. Если спрашивают про анализы или диагнозы — объясни без страшных слов и подскажи, что делать дальше."""

FOLLOWUP_PROMPT = """Ты — врач-консультант высшей категории. Пациент обращается повторно с новой информацией или новой ситуацией.

ДАННЫЕ ПАЦИЕНТА (из опроса):
{survey_data}

ПРЕДЫДУЩИЙ АНАЛИЗ (для справки — НЕ смешивай с новым обращением):
{previous_analysis}

НОВОЕ ОБРАЩЕНИЕ ПАЦИЕНТА:
{followup_question}

ОТВЕТЫ НА УТОЧНЯЮЩИЕ ВОПРОСЫ ПО НОВОМУ ОБРАЩЕНИЮ:
{followup_qa}

КРИТИЧЕСКИ ВАЖНО:
- Если пациент описывает НОВУЮ ситуацию, симптомы или проблему — анализируй её ОТДЕЛЬНО, как самостоятельный случай. НЕ смешивай с предыдущим анализом.
- Предыдущий анализ используй ТОЛЬКО как фоновый контекст о пациенте (возраст, пол, хронические заболевания, образ жизни), но НЕ переноси выводы и рекомендации из предыдущего случая на новый.
- Если новое обращение СВЯЗАНО с предыдущим (пациент сам об этом говорит или это очевидно из контекста) — тогда укажи связь, но всё равно анализируй новую информацию как основной фокус.

ЗАДАЧА:
1. Проанализируй НОВУЮ ситуацию пациента конкретно и по существу.
2. Если пациент спрашивает о враче — укажи конкретную специальность и почему именно к нему.
3. Если спрашивает об анализах — перечисли конкретные анализы, зачем каждый нужен.
4. В конце ответа укажи, какие документы ещё помогли бы уточнить картину (если есть такие).

Формат:
- Сначала прямой ответ/анализ по НОВОЙ ситуации
- Затем блок «Для более точного анализа было бы полезно загрузить:» (если применимо)

НИКОГДА не используй LaTeX, $, \\text{{}}. Числа пиши обычным текстом (126 г/л, 10⁹/л).
Пиши по-русски, простыми словами, обращаясь на «вы»."""

REQUEST_ANALYSIS_PROMPT = """Ты — опытный врач-диагност высшей категории с 30-летним стажем. Тебе поступил запрос от пациента.

ДАННЫЕ ПАЦИЕНТА (из опроса):
{survey_data}

ОПИСАНИЕ СИТУАЦИИ ОТ ПАЦИЕНТА:
{patient_request}

ОТВЕТЫ НА УТОЧНЯЮЩИЕ ВОПРОСЫ:
{followup_qa}

Твоя задача: собрать воедино всю имеющуюся информацию и представить её пациенту в структурированном виде, а затем рекомендовать загрузить документы для полного анализа.

ОФОРМЛЕНИЕ: все числа — только цифрами (возраст 39, рост 186 см, вес 110 кг). Возраст рассчитай от 2026 по году рождения. Каждый пункт с новой строки.

Ответь СТРОГО по следующей структуре (каждый раздел — отдельный абзац с заголовком):

**Ваши данные:**
Каждый пункт с новой строки:
• ФИО
• Возраст: цифрой (например 39 лет), рассчитай от 2026 по году рождения
• Пол
• Рост: цифрами, см
• Вес: цифрами, кг

**Контекст ситуации:**
Что произошло, когда, обстоятельства (из описания пациента и ответов на вопросы). Кратко, по фактам.

**Ваши жалобы:**
Перечисли все симптомы и жалобы, которые указал пациент (из описания и ответов на вопросы). Кратко, списком.

**Ваш запрос:**
Суть обращения — что пациент хочет узнать / получить. Одно-два предложения.

**Необходимые документы:**
Для полноценного анализа рекомендую загрузить (выбери только релевантные из списка):
- Результаты обследований (УЗИ, КТ, МРТ, ЭКГ, рентген)
- Анализы (кровь, моча, биохимия и др.)
- Выписной эпикриз
- Консультативное заключение врача
- Назначения и рецепты
- Протокол СМП (скорой помощи)
- История болезни / амбулаторная карта

Пиши по-русски, простыми словами, обращаясь на «вы». Не ставь диагноз. Не пугай. Будь конкретен."""

MAX_ADAPTIVE_QUESTIONS = 10

ADAPTIVE_QUESTION_PROMPT = """Ты — опытный и внимательный врач-диагност. Ты ведёшь живой диалог с пациентом, как настоящий доктор на приёме.

ДАННЫЕ ПАЦИЕНТА (из анкеты):
{survey_data}

ИСТОРИЯ ПРЕДЫДУЩИХ ОБРАЩЕНИЙ (хронология):
{patient_history}

ТЕКУЩЕЕ ОБРАЩЕНИЕ ПАЦИЕНТА:
{patient_request}

УЖЕ ЗАДАННЫЕ ВОПРОСЫ И ОТВЕТЫ (в этом диалоге):
{qa_so_far}

Номер текущего вопроса: {question_number} (максимум {max_questions})

ТВОЯ ЗАДАЧА:
Ты должен решить: задать ЕЩЁ ОДИН уточняющий вопрос или закончить опрос.

ПРИНЦИП ПРИНЯТИЯ РЕШЕНИЯ (как настоящий врач):
- Задавай вопросы только если тебе реально не хватает информации для точного заключения.
- Каждый следующий вопрос выбирай ИСХОДЯ ИЗ ВСЕХ полученных ответов — адаптируйся к новому контексту.
- Если пациент описал новую ситуацию, а у него уже были предыдущие обращения — сначала уточни, связана ли новая жалоба с предыдущими (если это не очевидно).
- НЕ задавай вопросы, ответы на которые уже есть в анкете, истории или предыдущих ответах.
- Если ты уже понимаешь картину достаточно хорошо для безопасных рекомендаций — ЗАКАНЧИВАЙ. Не затягивай опрос ради опроса.
- Минимум вопросов — 1, максимум — {max_questions}. Спрашивай столько, сколько РЕАЛЬНО нужно.

ФОРМАТ ОТВЕТА — строго JSON, без обёрток, без markdown:

Если нужен ещё вопрос:
{{"q": "Текст вопроса", "options": ["вариант1", "вариант2"]}}

Если информации достаточно:
{{"done": true}}

ПРАВИЛА ДЛЯ ВОПРОСОВ:
- СТРОГО по-русски, на «вы». Никаких иероглифов или нелатинских символов кроме кириллицы.
- Вопросы Да/Нет (содержат «ли вы», «были ли», «есть ли» и т.п.): options обязательно ["Да", "Нет"] (или + "Не помню").
- Альтернативные вопросы (с «или»): укажи варианты выбора в options (НЕ "Да"/"Нет").
- Выбор из вариантов (сроки, место, интенсивность): 2–4 варианта в options.
- Открытый вопрос (нужен развёрнутый ответ): options пустой [], но ОБЯЗАТЕЛЬНО добавь подсказку в скобках (примеры ответов), чтобы пациент понимал, что писать.
- Будь ЧЕЛОВЕЧНЫМ: не допрашивай, а беседуй. Формулируй мягко, понятно для пожилого человека."""

# Уточняющие вопросы после анализа документов (1–5 вопросов для уточнения заключения)
POST_DOC_QUESTIONS_PROMPT = """Ты — врач-диагност. По документам пациента уже проведён первичный анализ. Чтобы уточнить заключение и рекомендации, нужно задать пациенту от 1 до 5 коротких уточняющих вопросов.

ЗАПРОС ПАЦИЕНТА:
{patient_request}

ПЕРВИЧНЫЙ АНАЛИЗ (кратко):
{analysis_summary}

Сформулируй от 1 до 5 самых важных вопросов, на которые нужны ответы пациента (например: текущие симптомы, приём лекарств, дата последнего обследования, аллергии, что уже пробовал и т.д.). Вопросы должны быть краткими и конкретными.

ВАЖНО: для открытых вопросов ОБЯЗАТЕЛЬНО добавляй подсказку в скобках с примерами возможных ответов. Пациенты часто не знают, что и как описать. Примеры: «Опишите характер боли (давящая, колющая, жгучая, тупая, ноющая)», «Какие лекарства принимаете? (название, дозировка, частота приёма)».

Формат ответа: строго по одному вопросу на строку, без нумерации. Не пиши вводных фраз — только вопросы, каждый с новой строки. По-русски, на «вы»."""

# Уточнение заключения с учётом ответов пациента на уточняющие вопросы
REFINED_ANALYSIS_PROMPT = """Ты — врач-диагност. У тебя есть первичный анализ по документам пациента и ответы пациента на уточняющие вопросы. Дай итоговое заключение и рекомендации с учётом этой дополнительной информации.

ПЕРВИЧНЫЙ АНАЛИЗ:
{full_analysis}

УТОЧНЯЮЩИЕ ВОПРОСЫ И ОТВЕТЫ ПАЦИЕНТА:
{qa_block}

ЗАДАЧА: сохрани структуру первичного анализа, но дополни и скорректируй разделы с учётом ответов пациента. Если ответы меняют выводы или рекомендации — укажи это явно.

ОФОРМЛЕНИЕ: все числа — только цифрами (возраст 39, рост 186 см, вес 110 кг, показатели анализов). Возраст — цифрой, рассчитай от текущего года по году рождения. Каждый пункт с новой строки. Пиши по-русски. Не используй LaTeX. Будь конкретен."""

INTENT_CLASSIFY_PROMPT = """Пользователь общается с медицинским ботом. Бот попросил загрузить медицинские документы (анализы, заключения, выписки).
Пользователь написал: "{user_text}"

Определи намерение — ответь ОДНИМ словом:
- no_docs — пользователь говорит что документов нет (в любой форме: "нет документов", "доков нема", "ничего нет на руках", "не имею" и т.п.)
- question — пользователь задаёт медицинский вопрос или просит совет по здоровью
- other — что-то иное, не подходящее ни к одному из вариантов выше

Ответ (одно слово):"""

FULL_ANALYSIS_PROMPT = """Ты — врач-диагност высшей категории и профильный специалист. Твоя задача: собрать ВСЮ имеющуюся информацию о пациенте и выдать итоговое заключение («итог») по строго заданному плану.

ИСХОДНЫЕ ДАННЫЕ:

Данные из опроса (анкета):
{survey_data}

Описание ситуации / запрос пациента:
{patient_request}

Ответы пациента на уточняющие вопросы (вопрос — ответ):
{followup_qa}

{documents_section}

---

ФОРМАТИРОВАНИЕ (ОБЯЗАТЕЛЬНО):
- Все числа — только цифрами: возраст (39), рост (186 см), вес (110 кг), показатели анализов, даты, дозировки. Никогда не пиши числа словами («тридцать девять», «сто десять» и т.п.).
- Возраст: рассчитай от текущего года (2026) по году рождения из опроса. Пиши только цифрой: «39 лет», не «тридцать девять лет».
- Каждый пункт или факт — с новой строки. Внутри раздела используй списки (• или -). Не сливай всё в один абзац.
- Выделяй подзаголовки жирным (**подзаголовок**). Официальный, но аккуратный стиль.

ЗАДАЧА: выдать ИТОГ строго по следующей структуре (8 пунктов). Каждый пункт — отдельный блок с заголовком. Если данных для пункта нет — кратко укажи «Данных нет» и переходи к следующему.

**1. ИНФОРМАЦИЯ О КЛИЕНТЕ**
Структурированно, каждый пункт с новой строки:
• **ФИО:** (из опроса)
• **Возраст:** только цифрой, рассчитай от 2026 по году рождения (например: 39 лет)
• **Пол:** мужской / женский
• **Рост:** цифрами, см (например: 186 см)
• **Вес:** цифрами, кг (например: 110 кг)
• Хронические болезни, лекарства, аллергии — кратко, списком, числа цифрами

**2. КОНТЕКСТ СЛУЧИВШЕГОСЯ И ЗАПРОС**
Что произошло, с чем человек обратился, хронология (даты и сроки — цифрами). Суть запроса. Каждый факт с новой строки.

**3. СИМПТОМЫ И ФАКТЫ**
Все симптомы и жалобы — списком, каждый с новой строки (•). Без интерпретации, только факты. Числа и сроки — цифрами.

**4. ЦИФРОВЫЕ ПОКАЗАТЕЛИ: НОРМА, ОТКЛОНЕНИЯ, ИНТЕРПРЕТАЦИЯ**
По каждому показателю: значение у пациента (цифрой) → норма (цифрой) → отклонение в % или «в норме» → краткая интерпретация. Группируй по системам. Каждый показатель с новой строки. Если документов нет — «Данных нет, рекомендуются базовые анализы по показаниям».

**5. ОЦЕНКА ДЕЙСТВИЙ ВРАЧЕЙ**
Если по документам или описанию были обращения к врачам, обследования, назначения — перечисли, что уже сделано, и дай краткую оценку (адекватно ли, чего не хватает). Если обращений не было — так и напиши.

**6. ЧТО НЕ БЫЛО СДЕЛАНО, А ДОЛЖНО БЫЛО БЫ**
По стандартам помощи: какие обследования, консультации или назначения должны были быть, но не отражены в данных. Конкретно, по пунктам. Если всё необходимое уже сделано — кратко укажи это.

**7. РЕКОМЕНДАЦИИ: БЛИЖАЙШИЕ ДЕЙСТВИЯ И ЛЕЧЕНИЕ**
Что делать в первую очередь: шаги по порядку (обратиться к врачу, дообследование, режим, наблюдение). Срочность: экстренно / плановая консультация / можно наблюдать дома. Ясным языком, чтобы пациент мог выполнить.

**8. РЕКОМЕНДАЦИИ: ЛЕКАРСТВА И ПРОЦЕДУРЫ**
Конкретные препараты (название, форма, доза, длительность — по возможности), процедуры, манипуляции. Противопоказания и чего избегать, с учётом данных опроса (аллергии, сопутствующие болезни).

Пиши по-русски. Не используй LaTeX, формулы, $ или \\text{{}}. Все числа — только цифрами (126 г/л, 7,76×10⁹/л, 15 мм/час); для степеней — ⁰¹²³⁴⁵⁶⁷⁸⁹. Никогда не заменяй цифры словами. Будь конкретен. Не пугай, но не скрывай важное."""

# Ключи в user_data для буфера фото
PENDING_IMAGES_KEY = "pending_images"
PENDING_CHAT_ID_KEY = "pending_chat_id"
BATCH_DELAY_SEC = 10


GROQ_VISION_MODEL = "meta-llama/llama-4-scout-17b-16e-instruct"
GROQ_TEXT_MODEL = "llama-3.3-70b-versatile"


def _use_groq() -> bool:
    return bool(os.getenv("GROQ_API_KEY"))


def get_groq_client() -> Optional[OpenAI]:
    api_key = os.getenv("GROQ_API_KEY")
    if not api_key:
        return None
    return OpenAI(base_url="https://api.groq.com/openai/v1", api_key=api_key)


def get_openai_client() -> Optional[OpenAI]:
    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        return None
    return OpenAI(api_key=api_key)


def _no_ai_message() -> str:
    return "Не задан ни один ключ нейросети. Добавь в .env: GROQ_API_KEY (console.groq.com) или OPENAI_API_KEY (platform.openai.com)."


def _short_error(e: Exception) -> str:
    msg = str(e).strip()
    if "429" in msg or "quota" in msg.lower() or "insufficient_quota" in msg:
        return "Закончился лимит по ключу. Groq: console.groq.com; OpenAI: platform.openai.com."
    if "401" in msg or "invalid" in msg.lower() or "api_key" in msg.lower():
        return "Неверный или недействительный API-ключ. Проверь ключ в .env."
    if "access denied" in msg.lower() or "network" in msg.lower():
        return "Доступ к API заблокирован (сеть или регион). Попробуй другой ключ или VPN."
    if len(msg) > 200:
        return msg[:197] + "..."
    return msg


def _escape_html(s: str) -> str:
    return (s or "").replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")


def _strip_foreign_chars(text: str) -> str:
    """Убирает иероглифы и прочие нелатинские/некириллические символы."""
    return re.sub(r'[\u2E80-\u9FFF\uF900-\uFAFF\uFE30-\uFE4F\U00020000-\U0002FA1F\u0600-\u06FF\u0900-\u097F\u3040-\u309F\u30A0-\u30FF]', '', text)


def _strip_latex(text: str) -> str:
    """Убирает LaTeX-разметку: $...$, \text{}, \times и т.д."""
    text = re.sub(r"\$([^$]*)\$", r"\1", text)
    text = re.sub(r"\\text\{([^}]*)\}", r"\1", text)
    text = text.replace("\\times", "×")
    text = text.replace("\\cdot", "·")
    text = text.replace("\\leq", "≤").replace("\\geq", "≥")
    text = text.replace("\\lt", "<").replace("\\gt", ">")
    text = text.replace("\\le", "≤").replace("\\ge", "≥")
    text = re.sub(r"\^(\d)", lambda m: "⁰¹²³⁴⁵⁶⁷⁸⁹"[int(m.group(1))], text)
    text = re.sub(r"\^\{(\d+)\}", lambda m: "".join("⁰¹²³⁴⁵⁶⁷⁸⁹"[int(d)] for d in m.group(1)), text)
    text = re.sub(r"\\[a-zA-Z]+", "", text)
    return text


def _format_conclusion_for_elderly(raw: str) -> str:
    """Форматирует заключение для удобного чтения: заголовки, эмодзи, жирный текст, разбивка."""
    if not raw or len(raw) > 4000:
        raw = (raw or "")[:4000]
    raw = _strip_latex(raw)
    raw = _escape_html(raw)
    raw = re.sub(r"\*\*(.+?)\*\*", r"<b>\1</b>", raw)

    section_icons = {
        "ИНФОРМАЦИЯ О КЛИЕНТЕ": "👤",
        "КОНТЕКСТ СЛУЧИВШЕГОСЯ И ЗАПРОС": "📌",
        "СИМПТОМЫ И ФАКТЫ": "📋",
        "ЦИФРОВЫЕ ПОКАЗАТЕЛИ: НОРМА, ОТКЛОНЕНИЯ, ИНТЕРПРЕТАЦИЯ": "🔬",
        "ОЦЕНКА ДЕЙСТВИЙ ВРАЧЕЙ": "👨‍⚕️",
        "ЧТО НЕ БЫЛО СДЕЛАНО, А ДОЛЖНО БЫЛО БЫ": "⚠️",
        "РЕКОМЕНДАЦИИ: БЛИЖАЙШИЕ ДЕЙСТВИЯ И ЛЕЧЕНИЕ": "📝",
        "РЕКОМЕНДАЦИИ: ЛЕКАРСТВА И ПРОЦЕДУРЫ": "💊",
        "ПРОФЕССИОНАЛЬНЫЙ БЛОК": "🏥",
        "РЕЗЮМЕ СЛУЧАЯ": "📋",
        "КЛИНИЧЕСКАЯ ИНТЕРПРЕТАЦИЯ": "🔬",
        "ДИАГНОЗ": "🩺",
        "ДИФФЕРЕНЦИАЛЬНЫЙ ДИАГНОЗ": "🔍",
        "ПАЦИЕНТСКИЙ БЛОК": "💬",
        "ЧТО ЭТО ЗНАЧИТ ДЛЯ ВАС": "💡",
        "СРОЧНОСТЬ": "🚨",
        "ЧТО СДЕЛАЛИ ВРАЧИ": "👨‍⚕️",
        "ДЕЙСТВИЯ ВРАЧЕЙ — ЧТО БЫЛО СДЕЛАНО": "✅",
        "ЧТО ВРАЧИ НЕ СДЕЛАЛИ — О ЧЁМ НУЖНО СПРОСИТЬ": "⚠️",
        "ПЛАН ДЕЙСТВИЙ": "📝",
        "ЧТО ДЕЛАТЬ": "🎯",
    }
    for title, icon in section_icons.items():
        raw = raw.replace(title, f"\n\n{icon} <b>{title}</b>\n")

    raw = re.sub(r"(?m)^#+\s*(.+)$", r"\n<b>\1</b>\n", raw)
    raw = re.sub(r"(?m)^(\d+)\.\s+", r"\n\1️⃣ ", raw)
    raw = re.sub(r"(\d+)\)\s*", r"\n\1️⃣ ", raw)
    # Списки: отступ (табуляция) и маркер • для аккуратного вида
    raw = re.sub(r"(?m)^-\s+", "    • ", raw)
    raw = re.sub(r"(?m)^•\s+", "    • ", raw)
    raw = re.sub(r"(?m)^#+\s*", "", raw)
    raw = re.sub(r"\n{3,}", "\n\n", raw)
    raw = raw.strip()
    if len(raw) > 4080:
        raw = raw[:4077] + "..."
    return raw


async def _send_long_html(bot, chat_id: int, text: str, reply_markup=None) -> None:
    """Отправляет длинный HTML-текст частями (лимит Telegram 4096)."""
    chunk_size = 4000
    for i in range(0, len(text), chunk_size):
        chunk = text[i : i + chunk_size]
        markdown = reply_markup if (i + chunk_size >= len(text)) else None
        await bot.send_message(chat_id, chunk, parse_mode="HTML", reply_markup=markdown)


def _save_conclusion(user_id: int, conclusion: str) -> None:
    """Сохраняет заключение по user_id: разбивает на диагноз и рекомендации по лечению."""
    conclusion = (conclusion or "").strip()
    if not conclusion:
        return
    diagnosis = conclusion
    treatment = ""
    if "АБЗАЦ 2" in conclusion:
        parts = conclusion.split("АБЗАЦ 2", 1)
        diagnosis = parts[0].strip()
        treatment = ("АБЗАЦ 2 " + parts[1]).strip() if len(parts) > 1 else ""
    if user_id not in _user_last:
        _user_last[user_id] = {"diagnosis": "", "treatment": ""}
    _user_last[user_id]["diagnosis"] = diagnosis[:4000]
    _user_last[user_id]["treatment"] = treatment[:4000] if treatment else conclusion[:4000]


async def _ask_openai_image(image_b64: str, mime: str = "image/jpeg") -> str:
    client = get_openai_client()
    if not client:
        return ""
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {"role": "system", "content": MEDICAL_PROMPT},
            {
                "role": "user",
                "content": [
                    {"type": "text", "text": "Объясни этот медицинский документ простыми словами по пунктам из инструкции."},
                    {"type": "image_url", "image_url": {"url": f"data:{mime};base64,{image_b64}"}},
                ],
            },
        ],
        max_tokens=1500,
    )
    return (response.choices[0].message.content or "").strip()


async def _ask_openai_text(user_text: str) -> str:
    client = get_openai_client()
    if not client:
        return ""
    response = client.chat.completions.create(
        model="gpt-4o-mini",
        messages=[
            {"role": "system", "content": TEXT_PROMPT},
            {"role": "user", "content": user_text},
        ],
        max_tokens=1000,
    )
    return (response.choices[0].message.content or "").strip()


def _build_multi_content(images: list) -> list:
    """Список content для OpenAI/Groq: текст + все картинки."""
    parts = [{"type": "text", "text": "По всем приложенным документам сделай одно заключение по инструкции (два абзаца, пункты 1-2-3)."}]
    for b64, mime in images:
        parts.append({"type": "image_url", "image_url": {"url": f"data:{mime};base64,{b64}"}})
    return parts


async def _ask_groq_images(images: list) -> str:
    """images: список кортежей (image_b64, mime)."""
    client = get_groq_client()
    if not client or not images:
        return ""
    content = _build_multi_content(images)
    response = client.chat.completions.create(
        model=GROQ_VISION_MODEL,
        messages=[
            {"role": "system", "content": MULTI_DOC_PROMPT},
            {"role": "user", "content": content},
        ],
        max_tokens=2000,
    )
    return (response.choices[0].message.content or "").strip()


async def _ask_openai_images(images: list) -> str:
    """images: список кортежей (image_b64, mime)."""
    client = get_openai_client()
    if not client or not images:
        return ""
    content = _build_multi_content(images)
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {"role": "system", "content": MULTI_DOC_PROMPT},
            {"role": "user", "content": content},
        ],
        max_tokens=2000,
    )
    return (response.choices[0].message.content or "").strip()


async def _ask_groq_image(image_b64: str, mime: str = "image/jpeg") -> str:
    client = get_groq_client()
    if not client:
        return ""
    response = client.chat.completions.create(
        model=GROQ_VISION_MODEL,
        messages=[
            {"role": "system", "content": MEDICAL_PROMPT},
            {
                "role": "user",
                "content": [
                    {"type": "text", "text": "Объясни этот медицинский документ простыми словами по пунктам из инструкции."},
                    {"type": "image_url", "image_url": {"url": f"data:{mime};base64,{image_b64}"}},
                ],
            },
        ],
        max_tokens=1500,
    )
    return (response.choices[0].message.content or "").strip()


def _format_survey_data(answers: dict) -> str:
    """Форматирует ответы опроса в читаемый текст для промпта ИИ."""
    if not answers:
        return "Нет данных опроса."
    lines = []
    for i, (q_tuple) in enumerate(MEDICAL_QUESTIONS):
        q_text = q_tuple[0]
        val = answers.get(f"q{i+1}", "").strip()
        if val:
            lines.append(f"- {q_text}: {val}")
    return "\n".join(lines) if lines else "Нет данных опроса."


def _get_patient_address(fio: str) -> str:
    """
    Разбирает ФИО (Фамилия Имя Отчество) и возвращает форму обращения:
    - 3+ слова → Имя Отчество
    - 2 слова  → второе слово (Имя)
    - 1 слово  → «Уважаемый»
    - пусто    → ""
    """
    parts = fio.strip().split()
    if len(parts) >= 3:
        return f"{parts[1]} {parts[2]}"
    if len(parts) == 2:
        return parts[1]
    if len(parts) == 1:
        return "Уважаемый"
    return "Уважаемый клиент"


import json as _json

_YES_NO_PATTERNS = re.compile(
    r"^(были\s+ли|есть\s+ли|имеется\s+ли|имеются\s+ли|принимали\s+ли|"
    r"принимаете\s+ли|делали\s+ли|испытываете\s+ли|ощущаете\s+ли|"
    r"наблюдали\s+ли|наблюдаете\s+ли|замечали\s+ли|замечаете\s+ли|"
    r"проводили\s+ли|обращались\s+ли|страдаете\s+ли|болели\s+ли|"
    r"проходили\s+ли|сдавали\s+ли|назначали\s+ли|беспокоит\s+ли)",
    re.IGNORECASE,
)


def _auto_add_yes_no(q_text: str, options: List[str]) -> List[str]:
    """Если вопрос явно подразумевает Да/Нет, а options пуст — добавляет их.
    Не добавляет для альтернативных вопросов (содержащих «или»)."""
    if options:
        return options
    stripped = q_text.strip()
    if re.search(r"\bили\b", stripped, re.IGNORECASE):
        return options
    if _YES_NO_PATTERNS.search(stripped):
        return ["Да", "Нет"]
    return options


def _parse_questions_from_ai(response: str) -> List[Dict[str, Any]]:
    """Парсит JSON-массив вопросов от ИИ. Возвращает список {"q": str, "options": list[str]}.
    Fallback: если JSON не парсится — обрабатывает построчно (без options)."""
    if not response or not response.strip():
        return []
    text = response.strip()
    text = re.sub(r"^```(?:json)?\s*", "", text)
    text = re.sub(r"\s*```$", "", text)
    try:
        data = _json.loads(text)
        if isinstance(data, list):
            result = []
            for item in data[:5]:
                if isinstance(item, dict) and "q" in item:
                    opts = item.get("options") or []
                    if not isinstance(opts, list):
                        opts = []
                    opts = [str(o).strip() for o in opts if str(o).strip()]
                    q_text = str(item["q"]).strip()
                    opts = _auto_add_yes_no(q_text, opts)
                    result.append({"q": q_text, "options": opts})
                elif isinstance(item, str) and len(item.strip()) > 3:
                    q_text = item.strip()
                    result.append({"q": q_text, "options": _auto_add_yes_no(q_text, [])})
            return result
    except (_json.JSONDecodeError, TypeError, ValueError):
        pass
    lines = [line.strip() for line in text.splitlines() if line.strip()]
    cleaned: List[Dict[str, Any]] = []
    for line in lines:
        s = re.sub(r"^\s*\d+[\.\)]\s*", "", line).strip()
        if s and len(s) > 3:
            cleaned.append({"q": s, "options": _auto_add_yes_no(s, [])})
    return cleaned[:5]


def _parse_adaptive_question(response: str) -> Optional[Dict[str, Any]]:
    """Парсит ответ адаптивного вопроса: возвращает {"q": ..., "options": [...]} или None если done."""
    if not response or not response.strip():
        return None
    text = response.strip()
    text = re.sub(r"^```(?:json)?\s*", "", text)
    text = re.sub(r"\s*```$", "", text)
    try:
        data = _json.loads(text)
        if isinstance(data, dict):
            if data.get("done"):
                return None
            if "q" in data:
                opts = data.get("options") or []
                if not isinstance(opts, list):
                    opts = []
                opts = [str(o).strip() for o in opts if str(o).strip()]
                q_text = str(data["q"]).strip()
                if q_text and len(q_text) > 3:
                    opts = _auto_add_yes_no(q_text, opts)
                    return {"q": q_text, "options": opts}
    except (_json.JSONDecodeError, TypeError, ValueError):
        pass
    lines = [l.strip() for l in text.splitlines() if l.strip() and len(l.strip()) > 5]
    if lines:
        q_text = re.sub(r"^\s*\d+[\.\)]\s*", "", lines[0]).strip()
        if q_text and len(q_text) > 3:
            return {"q": q_text, "options": _auto_add_yes_no(q_text, [])}
    return None


def _format_patient_history(history: List[Dict[str, str]]) -> str:
    """Форматирует историю обращений пациента для промптов."""
    if not history:
        return "Первое обращение пациента."
    lines = []
    for i, entry in enumerate(history, 1):
        date = entry.get("date", "?")
        situation = entry.get("situation", "")
        summary = entry.get("summary", "")
        lines.append(f"--- Обращение #{i} ({date}) ---\nСитуация: {situation}\nИтог: {summary}")
    return "\n\n".join(lines)


def _format_qa_so_far(questions: List[Dict[str, Any]], answers: Dict[int, str]) -> str:
    """Форматирует уже заданные вопросы и ответы для адаптивного промпта."""
    if not questions or not answers:
        return "Вопросы ещё не задавались."
    lines = []
    for i, q_data in enumerate(questions):
        q_text = q_data["q"] if isinstance(q_data, dict) else str(q_data)
        a_text = answers.get(i, "")
        if a_text:
            lines.append(f"В{i+1}: {q_text}\nО{i+1}: {a_text}")
    return "\n\n".join(lines) if lines else "Вопросы ещё не задавались."


async def _ask_ai_text(system_prompt: str, user_text: str) -> str:
    """Универсальный запрос к текстовому ИИ (Groq, затем OpenAI)."""
    text = ""
    client = get_groq_client()
    if client:
        try:
            resp = client.chat.completions.create(
                model=GROQ_TEXT_MODEL,
                messages=[
                    {"role": "system", "content": system_prompt},
                    {"role": "user", "content": user_text},
                ],
                max_tokens=2000,
            )
            text = (resp.choices[0].message.content or "").strip()
        except Exception as e:
            logger.warning("Groq text: %s", e)
    if not text:
        client = get_openai_client()
        if client:
            try:
                resp = client.chat.completions.create(
                    model="gpt-4o-mini",
                    messages=[
                        {"role": "system", "content": system_prompt},
                        {"role": "user", "content": user_text},
                    ],
                    max_tokens=2000,
                )
                text = (resp.choices[0].message.content or "").strip()
            except Exception as e:
                logger.warning("OpenAI text: %s", e)
    return text


async def _ask_ai_with_images(system_prompt: str, user_text: str, images: list) -> str:
    """Запрос к ИИ с текстом и изображениями (Groq, затем OpenAI)."""
    content = [{"type": "text", "text": user_text}]
    for b64, mime in images:
        content.append({"type": "image_url", "image_url": {"url": f"data:{mime};base64,{b64}"}})
    text = ""
    client = get_groq_client()
    if client:
        try:
            resp = client.chat.completions.create(
                model=GROQ_VISION_MODEL,
                messages=[
                    {"role": "system", "content": system_prompt},
                    {"role": "user", "content": content},
                ],
                max_tokens=3000,
            )
            text = (resp.choices[0].message.content or "").strip()
        except Exception as e:
            logger.warning("Groq vision: %s", e)
    if not text:
        client = get_openai_client()
        if client:
            try:
                resp = client.chat.completions.create(
                    model="gpt-4o",
                    messages=[
                        {"role": "system", "content": system_prompt},
                        {"role": "user", "content": content},
                    ],
                    max_tokens=3000,
                )
                text = (resp.choices[0].message.content or "").strip()
            except Exception as e:
                logger.warning("OpenAI vision: %s", e)
    return text


async def _ask_groq_text(user_text: str) -> str:
    client = get_groq_client()
    if not client:
        return ""
    response = client.chat.completions.create(
        model=GROQ_TEXT_MODEL,
        messages=[
            {"role": "system", "content": TEXT_PROMPT},
            {"role": "user", "content": user_text},
        ],
        max_tokens=1000,
    )
    return (response.choices[0].message.content or "").strip()


def _progress_bar(pct: int) -> str:
    """Полоска загрузки 0–100%."""
    n = 10
    filled = round(n * pct / 100)
    bar = "█" * filled + "░" * (n - filled)
    return f"[{bar}] {pct}%"


async def _progress_updater(bot: Any, chat_id: int, message_id: int, stop_event: asyncio.Event) -> None:
    """Обновляет сообщение с полосой загрузки каждую секунду (0→95%), пока не установлен stop_event."""
    for i in range(1, 16):
        if stop_event.is_set():
            return
        pct = min(95, i * 6)
        try:
            await bot.edit_message_text(
                chat_id=chat_id,
                message_id=message_id,
                text=f"Анализирую ваши данные…\n\n{_progress_bar(pct)}",
            )
        except Exception:
            pass
        await asyncio.sleep(1)
    # Держим 95% пока не скажут стоп
    while not stop_event.is_set():
        await asyncio.sleep(0.5)


async def _process_pending_images(
    context: ContextTypes.DEFAULT_TYPE, user_id: int, provider: Optional[str] = None
) -> None:
    """Скачать все фото из буфера, показать полосу загрузки, отправить в выбранную AI, выдать одно отформатированное сообщение.
    provider: "groq" | "openai" | None (None = сначала Groq, при ошибке OpenAI)."""
    data = _pending.pop(user_id, None)
    if not data:
        return
    chat_id = data["chat_id"]
    file_ids = data["file_ids"]
    if not file_ids:
        return
    bot = context.bot
    images_b64: List[tuple] = []
    for file_id, mime in file_ids:
        try:
            tg_file = await bot.get_file(file_id)
            buf = io.BytesIO()
            await tg_file.download_to_memory(buf)
            buf.seek(0)
            raw = buf.read()
            images_b64.append((base64.b64encode(raw).decode("utf-8"), mime))
        except Exception as e:
            logger.warning("Не удалось загрузить файл %s: %s", file_id, e)
    if not images_b64:
        await bot.send_message(chat_id, "Не удалось загрузить ни одного документа. Попробуй отправить снова.", reply_markup=MAIN_KEYBOARD)
        return

    # Одно сообщение «Анализирую ваши данные» с полосой загрузки
    progress_msg = await bot.send_message(
        chat_id,
        f"Анализирую ваши данные…\n\n{_progress_bar(0)}",
    )
    stop_event = asyncio.Event()
    progress_task = asyncio.create_task(_progress_updater(bot, chat_id, progress_msg.message_id, stop_event))

    text = ""
    last_err = None
    has_groq = _use_groq()
    has_openai = bool(get_openai_client())
    try:
        if provider == "groq" and has_groq:
            try:
                text = await _ask_groq_images(images_b64)
            except Exception as e:
                last_err = e
                logger.warning("Groq при разборе нескольких фото: %s", e)
        elif provider == "openai" and has_openai:
            try:
                text = await _ask_openai_images(images_b64)
            except Exception as e:
                last_err = e
                logger.warning("OpenAI при разборе нескольких фото: %s", e)
        else:
            # provider is None or не совпадает — пробуем Groq, потом OpenAI
            if has_groq:
                try:
                    text = await _ask_groq_images(images_b64)
                except Exception as e:
                    last_err = e
                    logger.warning("Groq при разборе нескольких фото: %s", e)
            if not text and has_openai:
                try:
                    text = await _ask_openai_images(images_b64)
                except Exception as e:
                    last_err = e
                    logger.warning("OpenAI при разборе нескольких фото: %s", e)
    finally:
        stop_event.set()
        progress_task.cancel()
        try:
            await progress_task
        except asyncio.CancelledError:
            pass
        try:
            await bot.edit_message_text(
                chat_id=chat_id,
                message_id=progress_msg.message_id,
                text=f"Анализирую ваши данные…\n\n{_progress_bar(100)}",
            )
        except Exception:
            pass
        await asyncio.sleep(0.3)
        try:
            await bot.delete_message(chat_id=chat_id, message_id=progress_msg.message_id)
        except Exception:
            pass

    if not text:
        await bot.send_message(
            chat_id,
            "Не удалось составить заключение. " + (_short_error(last_err) if last_err else "Проверь ключи в .env."),
            reply_markup=MAIN_KEYBOARD,
        )
        return
    if len(text) > 4000:
        text = text[:3997] + "..."
    _save_conclusion(user_id, text)
    formatted = _format_conclusion_for_elderly(text)
    await bot.send_message(
        chat_id,
        formatted,
        reply_markup=MAIN_KEYBOARD,
        parse_mode="HTML",
    )


async def _job_process_pending(context: ContextTypes.DEFAULT_TYPE) -> None:
    user_id = context.job.data
    await _process_pending_images(context, user_id, provider="groq")


async def start(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    for key in list(context.user_data.keys()):
        if key.startswith("awaiting_"):
            context.user_data.pop(key, None)

    welcome = (
        "<b>Привет!</b> 👋\n\n"
        "Я — помощник по медицинским документам. Разбираю анализы и заключения врачей простыми словами, "
        "чтобы вы и ваши близкие могли спокойно понять результаты и знать, что делать дальше.\n\n"
        "<b>Чем я полезен:</b>\n"
        "1️⃣ Объясняю анализы и выписки без сложных терминов\n"
        "2️⃣ Подсказываю, что в норме, а на что обратить внимание\n"
        "3️⃣ Даю понятные рекомендации: к врачу ли идти и о чём спросить\n"
        "4️⃣ Отвечаю на вопросы о здоровье простым языком\n\n"
        "Для начала войдите в свой аккаунт или зарегистрируйтесь."
    )
    auth_kb = InlineKeyboardMarkup([
        [InlineKeyboardButton("🔑 Авторизоваться", callback_data=CB_AUTH_LOGIN)],
        [InlineKeyboardButton("📝 Зарегистрироваться", callback_data=CB_AUTH_REGISTER)],
    ])
    await update.message.reply_text(
        welcome,
        reply_markup=auth_kb,
        parse_mode="HTML",
    )


def _next_step_keyboard() -> InlineKeyboardMarkup:
    """Кнопки после ввода имени: опрос или загрузить документы."""
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("📋 Пройти опрос", callback_data=CB_NEXT_SURVEY)],
        [InlineKeyboardButton("📎 Загрузить документы", callback_data=CB_NEXT_UPLOAD)],
    ])


def _survey_send_keyboard() -> InlineKeyboardMarkup:
    """Кнопка «Отправить ответ» в опросе."""
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("Отправить ответ", callback_data=CB_SURVEY_SEND)],
    ])


def _start_button_keyboard() -> InlineKeyboardMarkup:
    """Кнопка «Начать» после приветствия."""
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("Начать", callback_data=CB_FLOW_START)],
    ])


def _consent_keyboard() -> InlineKeyboardMarkup:
    """Согласен и продолжить / Не согласен для единого блока согласия."""
    return InlineKeyboardMarkup([
        [InlineKeyboardButton("Согласен и продолжить", callback_data=CB_CONSENT_ACCEPT)],
        [InlineKeyboardButton("Не согласен", callback_data=CB_CONSENT_DECLINE)],
    ])


async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    await update.message.reply_text(
        "/start — приветствие\n"
        "/help — эта справка\n\n"
        "Кнопки: Старт, Стоп, Перезапустить, Добавить фото, Диагноз (последнее заключение), Лечение (последние рекомендации).\n\n"
        "Можно прислать несколько фото подряд — через 10 сек разберу вместе (или выбери ИИ кнопкой). Или напиши «всё» / «готово». Вопрос текстом — отвечу простыми словами.",
        reply_markup=MAIN_KEYBOARD,
    )


def _schedule_pending_job(context: ContextTypes.DEFAULT_TYPE, user_id: int) -> None:
    """Запланировать разбор буфера через BATCH_DELAY_SEC: через job_queue или asyncio."""
    if context.job_queue:
        job_name = f"process_pending_{user_id}"
        for job in context.job_queue.jobs():
            if job.name == job_name:
                job.schedule_removal()
                break
        context.job_queue.run_once(
            _job_process_pending,
            when=BATCH_DELAY_SEC,
            data=user_id,
            name=job_name,
        )
        return
    # Без job_queue — отложенный запуск через asyncio
    old = _pending_tasks.pop(user_id, None)
    if old and not old.done():
        old.cancel()
    async def _delayed_batch(app: Any, uid: int) -> None:
        await asyncio.sleep(BATCH_DELAY_SEC)
        _pending_tasks.pop(uid, None)
        class _C:
            pass
        ctx = _C()
        ctx.bot = app.bot
        await _process_pending_images(ctx, uid, provider="groq")
    _pending_tasks[user_id] = asyncio.create_task(_delayed_batch(context.application, user_id))


def _add_to_pending(user_id: int, chat_id: int, file_id: str, mime: str) -> int:
    """Добавляет файл в буфер _pending, возвращает кол-во файлов."""
    if user_id not in _pending:
        _pending[user_id] = {"chat_id": chat_id, "file_ids": []}
    _pending[user_id]["chat_id"] = chat_id
    _pending[user_id]["file_ids"].append((file_id, mime))
    return len(_pending[user_id]["file_ids"])


async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    has_groq = _use_groq()
    has_openai = bool(get_openai_client())
    if not has_groq and not has_openai:
        await update.message.reply_text(_no_ai_message())
        return

    user_id = update.effective_user.id
    chat_id = update.effective_chat.id
    photo = update.message.photo[-1]
    n = _add_to_pending(user_id, chat_id, photo.file_id, "image/jpeg")

    keyboard = InlineKeyboardMarkup([
        [InlineKeyboardButton("📎 Отправить документы на анализ", callback_data=CB_SEND_DOCS)],
    ])
    if context.user_data.get("collecting_docs"):
        await update.message.reply_text(
            f"✅ Документ принят ({n}). Загрузите ещё или нажмите кнопку ниже.",
            reply_markup=keyboard,
        )
        return

    _schedule_pending_job(context, user_id)
    await update.message.reply_text(
        f"✅ Получил ({n}). Загрузите все документы и нажмите кнопку для анализа.",
        reply_markup=keyboard,
    )


async def handle_document(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    doc = update.message.document
    if doc.mime_type and not doc.mime_type.startswith("image/"):
        await update.message.reply_text(
            "Пока принимаю только фото и картинки (JPEG, PNG). PDF не поддерживаю — пришли, пожалуйста, скриншот страницы."
        )
        return

    has_groq = _use_groq()
    has_openai = bool(get_openai_client())
    if not has_groq and not has_openai:
        await update.message.reply_text(_no_ai_message())
        return

    user_id = update.effective_user.id
    chat_id = update.effective_chat.id
    mime = (doc.mime_type or "image/jpeg").strip()
    if mime not in ("image/jpeg", "image/png", "image/gif", "image/webp"):
        mime = "image/jpeg"

    n = _add_to_pending(user_id, chat_id, doc.file_id, mime)

    keyboard = InlineKeyboardMarkup([
        [InlineKeyboardButton("📎 Отправить документы на анализ", callback_data=CB_SEND_DOCS)],
    ])
    if context.user_data.get("collecting_docs"):
        await update.message.reply_text(
            f"✅ Документ принят ({n}). Загрузите ещё или нажмите кнопку ниже.",
            reply_markup=keyboard,
        )
        return

    _schedule_pending_job(context, user_id)
    await update.message.reply_text(
        f"✅ Получил ({n}). Загрузите все документы и нажмите кнопку для анализа.",
        reply_markup=keyboard,
    )


async def handle_ai_choice(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обработка нажатия кнопки выбора ИИ (Groq / OpenAI)."""
    query = update.callback_query
    await query.answer()
    data = (query.data or "").strip()
    user_id = query.from_user.id if query.from_user else 0
    if user_id not in _pending or not _pending[user_id].get("file_ids"):
        await query.edit_message_text("Документов для разбора нет. Пришлите фото или файлы.")
        return
    provider = None
    if data == CB_GROQ:
        provider = "groq"
    elif data == CB_OPENAI:
        provider = "openai"
    if not provider:
        return
    # Отменить отложенный запуск по таймеру
    if context.job_queue:
        job_name = f"process_pending_{user_id}"
        for job in context.job_queue.jobs():
            if job.name == job_name:
                job.schedule_removal()
                break
    task = _pending_tasks.pop(user_id, None)
    if task and not task.done():
        task.cancel()
    try:
        await query.edit_message_text("Запускаю анализ…")
    except Exception:
        pass
    await _process_pending_images(context, user_id, provider=provider)


def _format_medical_question(step: int, total: int) -> str:
    """Текст вопроса опросника."""
    q, variants = MEDICAL_QUESTIONS[step - 1]
    line = f"<b>Вопрос {step} из {total}</b>\n\n{q}"
    if variants:
        line += "\n\nВыберите вариант кнопкой или напишите свой."
    else:
        line += "\n\nНапишите ответ в чат и нажмите Enter."
    return line


def _survey_done_message(user_id_sheet) -> str:
    """Сообщение после завершения опроса с ID — просим описать запрос."""
    id_line = ""
    if user_id_sheet is not None:
        id_line = f"\n\nВаш уникальный <b>ID: {user_id_sheet}</b>. Сохраните его — он привязан к вашей карте."
    return (
        f"Спасибо за ваши ответы! Они помогут провести более точный анализ.{id_line}\n\n"
        "━━━━━━━━━━━━━━━━━━━━\n\n"
        "Теперь, пожалуйста, расскажите подробно о вашей ситуации:\n\n"
        "  — Что произошло? Что вас беспокоит?\n"
        "  — В связи с чем вы решили обратиться?\n"
        "  — Если у вас есть медицинские документы — расскажите, при каких обстоятельствах и по какой причине они были получены.\n\n"
        "Чем подробнее вы опишете ситуацию — симптомы, хронологию событий, "
        "обстоятельства — тем точнее и глубже я смогу провести анализ "
        "и дать полезные рекомендации.\n\n"
        "Напишите текстом или запишите голосовое сообщение."
    )


def _survey_question_keyboard(step: int) -> InlineKeyboardMarkup:
    """Инлайн-кнопки для вопроса: варианты ответа (если есть) + Пропустить."""
    _, variants = MEDICAL_QUESTIONS[step - 1]
    buttons: list[list[InlineKeyboardButton]] = []
    if variants:
        for i, v in enumerate(variants.split(" / ")):
            buttons.append([InlineKeyboardButton(v.strip(), callback_data=f"survey:ans:{i}")])
    buttons.append([InlineKeyboardButton("Пропустить", callback_data="survey:skip")])
    return InlineKeyboardMarkup(buttons)


async def _send_survey_question(
    bot, chat_id: int, step: int, total: int, *, remove_reply_kb: bool = False
) -> "telegram.Message":
    """Отправляет вопрос опросника с инлайн-кнопками. При remove_reply_kb убирает обычную клавиатуру."""
    q_text = _format_medical_question(step, total)
    kb = _survey_question_keyboard(step)
    if remove_reply_kb:
        sent = await bot.send_message(chat_id, q_text, parse_mode="HTML", reply_markup=ReplyKeyboardRemove())
        try:
            await bot.edit_message_reply_markup(chat_id=chat_id, message_id=sent.message_id, reply_markup=kb)
        except Exception:
            pass
    else:
        sent = await bot.send_message(chat_id, q_text, parse_mode="HTML", reply_markup=kb)
    return sent


async def handle_auth_choice(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Кнопки 'Авторизоваться' / 'Зарегистрироваться' после /start."""
    query = update.callback_query
    await query.answer()
    data = (query.data or "").strip()
    chat_id = query.message.chat_id
    bot = context.bot

    try:
        await query.message.delete()
    except Exception:
        pass

    for key in list(context.user_data.keys()):
        if key.startswith("awaiting_"):
            context.user_data.pop(key, None)

    if data == CB_AUTH_LOGIN:
        context.user_data["awaiting_login_email"] = True
        await bot.send_message(
            chat_id,
            "Введите ваш <b>email</b> (логин):",
            parse_mode="HTML",
            reply_markup=MAIN_KEYBOARD,
        )
    elif data == CB_AUTH_REGISTER:
        context.user_data["awaiting_reg_email"] = True
        await bot.send_message(
            chat_id,
            "Введите ваш <b>email</b> для регистрации:",
            parse_mode="HTML",
            reply_markup=MAIN_KEYBOARD,
        )
    elif data == CB_FORGOT_PASSWORD:
        saved_email = context.user_data.get("login_email", "").strip()
        if saved_email and "@" in saved_email:
            await bot.send_message(chat_id, "Сбрасываю пароль…", reply_markup=MAIN_KEYBOARD)
            new_pw = await asyncio.to_thread(_reset_user_password, saved_email)
            if new_pw:
                login_kb = InlineKeyboardMarkup([
                    [InlineKeyboardButton("🔑 Авторизоваться", callback_data=CB_AUTH_LOGIN)],
                ])
                await bot.send_message(
                    chat_id,
                    f"Новый пароль для <b>{_escape_html(saved_email)}</b>:\n\n"
                    f"<code>{new_pw}</code>\n\n"
                    "Запомните или сохраните его. Нажмите кнопку ниже, чтобы войти.",
                    parse_mode="HTML",
                    reply_markup=login_kb,
                )
            else:
                retry_kb = InlineKeyboardMarkup([
                    [InlineKeyboardButton("🔑 Авторизоваться", callback_data=CB_AUTH_LOGIN)],
                    [InlineKeyboardButton("📝 Зарегистрироваться", callback_data=CB_AUTH_REGISTER)],
                ])
                await bot.send_message(
                    chat_id,
                    "Пользователь с таким email не найден.\n\n"
                    "Проверьте email или зарегистрируйтесь.",
                    parse_mode="HTML",
                    reply_markup=retry_kb,
                )
        else:
            context.user_data["awaiting_reset_email"] = True
            await bot.send_message(
                chat_id,
                "Введите <b>email</b>, указанный при регистрации.\n"
                "Мы отправим на него новый пароль.",
                parse_mode="HTML",
                reply_markup=MAIN_KEYBOARD,
            )


async def handle_flow_start(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Кнопка «Начать» — удаляем приветствие, отправляем единый блок согласия."""
    query = update.callback_query
    await query.answer()
    try:
        await query.message.delete()
    except Exception:
        pass
    await context.bot.send_message(
        query.message.chat_id,
        CONSENT_TEXT,
        parse_mode="HTML",
        reply_markup=_consent_keyboard(),
    )


async def handle_consent(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Согласен и продолжить → первый вопрос опросника; Не согласен → стоп."""
    query = update.callback_query
    data = (query.data or "").strip()
    chat_id = query.message.chat_id
    bot = context.bot
    if data == CB_CONSENT_DECLINE:
        await query.answer()
        try:
            await query.message.delete()
        except Exception:
            pass
        await bot.send_message(
            chat_id,
            "Мы уважаем ваше решение.\n\n"
            "К сожалению, без согласия на обработку персональных данных и с условиями использования сервиса мы не можем предоставить возможность пользоваться ботом. "
            "Если измените решение — нажмите /start и примите условия.",
            reply_markup=MAIN_KEYBOARD,
        )
        return
    if data != CB_CONSENT_ACCEPT:
        return
    await query.answer()
    try:
        await query.message.delete()
    except Exception:
        pass
    context.user_data["survey_step"] = 1
    context.user_data["survey_answers"] = {}
    user = update.effective_user
    tg_username = f"@{user.username}" if user and user.username else (user.full_name if user else "")
    sheet_row, sheet_id, _ = await asyncio.to_thread(_sheet_start_row, tg_username, "")
    if sheet_row is not None and sheet_id is not None:
        context.user_data["survey_sheet_row"] = sheet_row
        context.user_data["survey_sheet_id"] = sheet_id
    else:
        context.user_data.pop("survey_sheet_row", None)
        context.user_data.pop("survey_sheet_id", None)
    total_q = len(MEDICAL_QUESTIONS)
    sent = await _send_survey_question(bot, chat_id, 1, total_q, remove_reply_kb=True)
    context.user_data["survey_question_message_id"] = sent.message_id


async def handle_next_step(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """«Пройти опрос» или «Загрузить документы» (если опросник запущен отдельно)."""
    query = update.callback_query
    await query.answer()
    data = (query.data or "").strip()
    if data == CB_NEXT_SURVEY:
        context.user_data["survey_step"] = 1
        context.user_data["survey_answers"] = {}
        user = update.effective_user
        tg_username = f"@{user.username}" if user and user.username else (user.full_name if user else "")
        sheet_row, sheet_id, _ = await asyncio.to_thread(_sheet_start_row, tg_username, "")
        if sheet_row is not None and sheet_id is not None:
            context.user_data["survey_sheet_row"] = sheet_row
            context.user_data["survey_sheet_id"] = sheet_id
        else:
            context.user_data.pop("survey_sheet_row", None)
            context.user_data.pop("survey_sheet_id", None)
        total = len(MEDICAL_QUESTIONS)
        chat_id = query.message.chat_id
        try:
            await query.message.delete()
        except Exception:
            pass
        sent = await _send_survey_question(context.bot, chat_id, 1, total, remove_reply_kb=True)
        context.user_data["survey_question_message_id"] = sent.message_id
    elif data == CB_NEXT_UPLOAD:
        upload_text = (
            "Пришлите фото или файл с анализом/заключением. Можно несколько — "
            "через 10 сек разберу вместе (бесплатный Groq) или нажмите кнопку и выберите ИИ."
        )
        try:
            await query.edit_message_text(upload_text, reply_markup=MAIN_KEYBOARD)
        except Exception:
            await query.message.reply_text(upload_text, reply_markup=MAIN_KEYBOARD)


async def handle_survey_callback(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обработка инлайн-кнопок опросника: выбор варианта (survey:ans:N), пропуск (survey:skip)."""
    query = update.callback_query
    data = (query.data or "").strip()
    step = context.user_data.get("survey_step", 0)
    if not step or step < 1 or step > len(MEDICAL_QUESTIONS):
        await query.answer()
        return

    if data == "survey:skip":
        answer_val = ""
    elif data.startswith("survey:ans:"):
        try:
            idx = int(data.split(":")[-1])
        except (ValueError, IndexError):
            await query.answer()
            return
        _, variants = MEDICAL_QUESTIONS[step - 1]
        if variants:
            opts = [v.strip() for v in variants.split(" / ")]
            answer_val = opts[idx] if idx < len(opts) else ""
        else:
            answer_val = ""
    else:
        await query.answer()
        return

    await query.answer()
    if "survey_answers" not in context.user_data:
        context.user_data["survey_answers"] = {}
    context.user_data["survey_answers"][f"q{step}"] = answer_val

    sheet_row = context.user_data.get("survey_sheet_row")
    if sheet_row is not None:
        await asyncio.to_thread(_sheet_update_answer, sheet_row, step, answer_val)

    chat_id = query.message.chat_id
    bot = context.bot
    try:
        await query.message.delete()
    except Exception:
        pass

    next_step = step + 1
    total = len(MEDICAL_QUESTIONS)
    if next_step <= total:
        context.user_data["survey_step"] = next_step
        sent = await _send_survey_question(bot, chat_id, next_step, total)
        context.user_data["survey_question_message_id"] = sent.message_id
    else:
        user_id_sheet = context.user_data.pop("survey_sheet_id", None)
        saved_answers = dict(context.user_data.get("survey_answers") or {})
        context.user_data.pop("survey_step", None)
        context.user_data.pop("survey_answers", None)
        context.user_data.pop("survey_question_message_id", None)
        context.user_data.pop("survey_sheet_row", None)
        context.user_data["completed_survey_answers"] = saved_answers
        context.user_data["awaiting_request"] = True
        await bot.send_message(
            chat_id,
            _survey_done_message(user_id_sheet),
            parse_mode="HTML",
            reply_markup=MAIN_KEYBOARD,
        )


async def handle_text(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    user_id = update.effective_user.id
    user_text = (update.message.text or "").strip()
    if not user_text:
        return

    # ---- Сброс пароля: ввод email ----
    if context.user_data.get("awaiting_reset_email"):
        context.user_data["awaiting_reset_email"] = False
        email = user_text.strip().lower()
        if "@" not in email or "." not in email:
            context.user_data["awaiting_reset_email"] = True
            await update.message.reply_text("Некорректный email. Попробуйте ещё раз:")
            return
        await update.message.reply_text("Проверяю...")
        new_pw = await asyncio.to_thread(_reset_user_password, email)
        if new_pw:
            login_kb = InlineKeyboardMarkup([
                [InlineKeyboardButton("🔑 Авторизоваться", callback_data=CB_AUTH_LOGIN)],
            ])
            await update.message.reply_text(
                f"Новый пароль для <b>{_escape_html(email)}</b>:\n\n"
                f"<code>{new_pw}</code>\n\n"
                "Запомните или сохраните его. Нажмите кнопку ниже, чтобы войти.",
                parse_mode="HTML",
                reply_markup=login_kb,
            )
        else:
            retry_kb = InlineKeyboardMarkup([
                [InlineKeyboardButton("🔑 Авторизоваться", callback_data=CB_AUTH_LOGIN)],
                [InlineKeyboardButton("📝 Зарегистрироваться", callback_data=CB_AUTH_REGISTER)],
            ])
            await update.message.reply_text(
                "Пользователь с таким email не найден.\n\n"
                "Проверьте email или зарегистрируйтесь.",
                parse_mode="HTML",
                reply_markup=retry_kb,
            )
        return

    # ---- Авторизация: ввод email ----
    if context.user_data.get("awaiting_login_email"):
        context.user_data["awaiting_login_email"] = False
        context.user_data["login_email"] = user_text.strip().lower()
        context.user_data["awaiting_login_password"] = True
        forgot_kb = InlineKeyboardMarkup([
            [InlineKeyboardButton("🔄 Забыли пароль?", callback_data=CB_FORGOT_PASSWORD)],
        ])
        await update.message.reply_text(
            "Введите <b>пароль</b>:",
            parse_mode="HTML",
            reply_markup=forgot_kb,
        )
        return

    # ---- Авторизация: ввод пароля ----
    if context.user_data.get("awaiting_login_password"):
        context.user_data["awaiting_login_password"] = False
        email = context.user_data.pop("login_email", "")
        password = user_text.strip()

        await update.message.reply_text("Проверяю…")
        user_rec = await asyncio.to_thread(_check_password, email, password)
        if user_rec:
            tg_user = update.effective_user
            tg_username = f"@{tg_user.username}" if tg_user and tg_user.username else (tg_user.full_name if tg_user else "")
            await asyncio.to_thread(_update_user_tg, email, user_id, tg_username)
            context.user_data["authenticated"] = True
            context.user_data["auth_email"] = email

            saved_survey = await asyncio.to_thread(_sheet_load_survey_by_tg, tg_username)
            if saved_survey:
                context.user_data["completed_survey_answers"] = saved_survey
                context.user_data["collecting_docs"] = True
                context.user_data["awaiting_request"] = True
                patient_address = _get_patient_address(saved_survey.get("q1", ""))
                greeting = f"Добро пожаловать, <b>{_escape_html(patient_address)}</b>! 👋" if patient_address else f"Добро пожаловать, <b>{_escape_html(email)}</b>! 👋"
                await update.message.reply_text(
                    f"{greeting}\n\n"
                    "Ваши данные из предыдущего опроса загружены.\n\n"
                    "Расскажите, что случилось? Что вас беспокоит?\n\n"
                    "Напишите текстом, запишите голосовое или загрузите медицинские документы.",
                    parse_mode="HTML",
                    reply_markup=MAIN_KEYBOARD,
                )
            else:
                chat_id = update.effective_chat.id
                bot = context.bot
                await update.message.reply_text(
                    f"Добро пожаловать, <b>{_escape_html(email)}</b>! 👋\n\n"
                    "Для точного анализа нужно заполнить короткую анкету.",
                    parse_mode="HTML",
                )
                context.user_data["survey_step"] = 1
                context.user_data["survey_answers"] = {}
                sheet_row, sheet_id, _ = await asyncio.to_thread(_sheet_start_row, tg_username, "")
                if sheet_row is not None and sheet_id is not None:
                    context.user_data["survey_sheet_row"] = sheet_row
                    context.user_data["survey_sheet_id"] = sheet_id
                total_q = len(MEDICAL_QUESTIONS)
                sent = await _send_survey_question(bot, chat_id, 1, total_q, remove_reply_kb=True)
                context.user_data["survey_question_message_id"] = sent.message_id
        else:
            retry_kb = InlineKeyboardMarkup([
                [InlineKeyboardButton("🔑 Авторизоваться", callback_data=CB_AUTH_LOGIN)],
                [InlineKeyboardButton("🔄 Забыли пароль?", callback_data=CB_FORGOT_PASSWORD)],
            ])
            await update.message.reply_text(
                "Неверный email или пароль, либо аккаунт не подтверждён.\n\n"
                "Выберите действие:",
                parse_mode="HTML",
                reply_markup=retry_kb,
            )
        return

    # ---- Регистрация: ввод email ----
    if context.user_data.get("awaiting_reg_email"):
        context.user_data["awaiting_reg_email"] = False
        email = user_text.strip().lower()
        if "@" not in email or "." not in email:
            context.user_data["awaiting_reg_email"] = True
            await update.message.reply_text("Некорректный email. Попробуйте ещё раз:")
            return
        existing = await asyncio.to_thread(_find_user_by_email, email)
        if existing:
            exists_kb = InlineKeyboardMarkup([
                [InlineKeyboardButton("🔑 Авторизоваться", callback_data=CB_AUTH_LOGIN)],
                [InlineKeyboardButton("🔄 Забыли пароль?", callback_data=CB_FORGOT_PASSWORD)],
                [InlineKeyboardButton("👤 Новый пользователь", callback_data=CB_AUTH_REGISTER)],
            ])
            await update.message.reply_text(
                "Пользователь с таким email уже зарегистрирован.\n\n"
                "Выберите действие:",
                reply_markup=exists_kb,
            )
            return
        context.user_data["reg_email"] = email
        context.user_data["awaiting_reg_password"] = True
        await update.message.reply_text("Придумайте <b>пароль</b> (минимум 4 символа):", parse_mode="HTML")
        return

    # ---- Регистрация: ввод пароля ----
    if context.user_data.get("awaiting_reg_password"):
        context.user_data["awaiting_reg_password"] = False
        password = user_text.strip()
        if len(password) < 4:
            context.user_data["awaiting_reg_password"] = True
            await update.message.reply_text("Пароль слишком короткий. Минимум 4 символа. Попробуйте ещё раз:")
            return
        email = context.user_data.get("reg_email", "")
        tg_user = update.effective_user
        tg_username = f"@{tg_user.username}" if tg_user and tg_user.username else (tg_user.full_name if tg_user else "")

        err = await asyncio.to_thread(_create_user, email, password, user_id, tg_username)
        if err:
            await update.message.reply_text(f"Ошибка регистрации: {err}\nПопробуйте /start заново.")
            return

        code = str(random.randint(100000, 999999))
        context.user_data["confirm_code"] = code
        context.user_data["confirm_email"] = email
        context.user_data["awaiting_confirm_code"] = True
        await update.message.reply_text(
            f"Ваш код подтверждения: <b>{code}</b>\n\n"
            "Введите этот код ниже для активации аккаунта.",
            parse_mode="HTML",
        )
        return

    # ---- Регистрация: ввод кода подтверждения ----
    if context.user_data.get("awaiting_confirm_code"):
        context.user_data["awaiting_confirm_code"] = False
        expected = context.user_data.pop("confirm_code", "")
        email = context.user_data.pop("confirm_email", "")
        if user_text.strip() == expected:
            ok = await asyncio.to_thread(_confirm_user, email)
            if ok:
                context.user_data["authenticated"] = True
                context.user_data["auth_email"] = email
                context.user_data.pop("reg_email", None)
                await update.message.reply_text(
                    "Аккаунт подтверждён! Регистрация завершена.\n\n"
                    "Теперь пройдём короткую процедуру согласия.",
                )
                await context.bot.send_message(
                    update.effective_chat.id,
                    CONSENT_TEXT,
                    parse_mode="HTML",
                    reply_markup=_consent_keyboard(),
                )
            else:
                await update.message.reply_text("Ошибка подтверждения. Попробуйте /start заново.")
        else:
            context.user_data["confirm_code"] = expected
            context.user_data["confirm_email"] = email
            context.user_data["awaiting_confirm_code"] = True
            await update.message.reply_text("Неверный код. Попробуйте ещё раз:")
        return

    # Ожидаем имя клиента после /start — не отправляем в ИИ, не ищем в интернете
    if context.user_data.get("awaiting_client_name"):
        context.user_data["awaiting_client_name"] = False
        context.user_data["client_name"] = user_text[:200]
        await update.message.reply_text(
            f"Записал: <b>{_escape_html(user_text[:200])}</b>.\n\nЧто делаем дальше?",
            reply_markup=_next_step_keyboard(),
            parse_mode="HTML",
        )
        return

    # Запрос пациента после опроса — анализ через ИИ-диагноста
    if context.user_data.get("awaiting_request"):
        await _handle_patient_request(update, context, user_text)
        return

    # Ответы на уточняющие вопросы ИИ (последовательно, по одному)
    if context.user_data.get("awaiting_clarify_answer"):
        await _advance_clarify(update.effective_chat.id, context, user_text)
        return

    # Ответы на уточняющие вопросы после анализа документов
    if context.user_data.get("awaiting_post_doc_answers"):
        context.user_data["awaiting_post_doc_answers"] = False
        context.user_data.pop("post_doc_followup_questions", None)
        full_analysis = context.user_data.get("full_analysis", "")
        if full_analysis:
            qa_block = "Ответы пациента на уточняющие вопросы:\n" + user_text[:2000]
            refine_prompt = REFINED_ANALYSIS_PROMPT.format(
                full_analysis=full_analysis[:6000],
                qa_block=qa_block,
            )
            await update.message.reply_text("Уточняю заключение с учётом ваших ответов…")
            refined = await _ask_ai_text(refine_prompt, qa_block)
            if refined:
                refined = _strip_latex(refined)
                context.user_data["full_analysis"] = refined
                _save_conclusion(update.effective_user.id, refined)
            keyboard = InlineKeyboardMarkup([
                [InlineKeyboardButton("📄 Показать результаты", callback_data=CB_SHOW_RESULTS)],
            ])
            await update.message.reply_text(
                "Готово. Нажмите кнопку ниже, чтобы увидеть итоговое заключение.",
                reply_markup=keyboard,
            )
        return

    # Ответ на вопрос опросника: ввёл текст и нажал Enter — сохраняем и сразу показываем следующий вопрос
    survey_step = context.user_data.get("survey_step")
    if survey_step and 1 <= survey_step <= len(MEDICAL_QUESTIONS):
        if "survey_answers" not in context.user_data:
            context.user_data["survey_answers"] = {}
        answer_val = user_text[:500]
        context.user_data["survey_answers"][f"q{survey_step}"] = answer_val
        sheet_row = context.user_data.get("survey_sheet_row")
        if sheet_row is not None:
            await asyncio.to_thread(_sheet_update_answer, sheet_row, survey_step, answer_val)
        chat_id = update.effective_chat.id
        bot = context.bot
        msg_id = context.user_data.get("survey_question_message_id")
        if msg_id:
            try:
                await bot.delete_message(chat_id=chat_id, message_id=msg_id)
            except Exception:
                pass
        next_step = survey_step + 1
        total = len(MEDICAL_QUESTIONS)
        if next_step <= total:
            context.user_data["survey_step"] = next_step
            sent = await _send_survey_question(bot, chat_id, next_step, total)
            context.user_data["survey_question_message_id"] = sent.message_id
        else:
            user_id_sheet = context.user_data.pop("survey_sheet_id", None)
            saved_answers = dict(context.user_data.get("survey_answers") or {})
            context.user_data.pop("survey_step", None)
            context.user_data.pop("survey_answers", None)
            context.user_data.pop("survey_question_message_id", None)
            context.user_data.pop("survey_sheet_row", None)
            context.user_data["completed_survey_answers"] = saved_answers
            context.user_data["awaiting_request"] = True
            await bot.send_message(
                chat_id,
                _survey_done_message(user_id_sheet),
                parse_mode="HTML",
                reply_markup=MAIN_KEYBOARD,
            )
        return

    # Кнопки: Старт, Стоп, Перезапустить, Добавить фото, Диагноз, Лечение
    if user_text == "Старт":
        await start(update, context)
        return
    if user_text == "Стоп":
        if context.job_queue:
            job_name = f"process_pending_{user_id}"
            for job in context.job_queue.jobs():
                if job.name == job_name:
                    job.schedule_removal()
                    break
        task = _pending_tasks.pop(user_id, None)
        if task and not task.done():
            task.cancel()
        _pending.pop(user_id, None)
        await update.message.reply_text("Остановлено. Буфер фото очищен.", reply_markup=MAIN_KEYBOARD)
        return
    if user_text == "Перезапустить":
        if context.job_queue:
            job_name = f"process_pending_{user_id}"
            for job in context.job_queue.jobs():
                if job.name == job_name:
                    job.schedule_removal()
                    break
        task = _pending_tasks.pop(user_id, None)
        if task and not task.done():
            task.cancel()
        _pending.pop(user_id, None)
        await update.message.reply_text(
            "Перезапуск. Буфер очищен. Можешь начать заново: пришли фото или нажми «Добавить фото».",
            reply_markup=MAIN_KEYBOARD,
        )
        return
    if user_text == "Добавить фото":
        await update.message.reply_text(
            "Пришли фото или файл с анализом/заключением. Можно несколько — через 10 сек разберу вместе или напиши «всё» / «готово».",
            reply_markup=MAIN_KEYBOARD,
        )
        return
    if user_text == "Диагноз":
        last = _user_last.get(user_id, {})
        diagnosis = (last.get("diagnosis") or "").strip()
        if diagnosis:
            await update.message.reply_text(diagnosis, reply_markup=MAIN_KEYBOARD)
        else:
            await update.message.reply_text("Пока нет сохранённого заключения. Пришли фото анализов/документов — разберу и сохраню.", reply_markup=MAIN_KEYBOARD)
        return
    if user_text == "Лечение":
        last = _user_last.get(user_id, {})
        treatment = (last.get("treatment") or last.get("diagnosis") or "").strip()
        if treatment:
            await update.message.reply_text(treatment, reply_markup=MAIN_KEYBOARD)
        else:
            await update.message.reply_text("Пока нет сохранённых рекомендаций по лечению. Пришли фото — после разбора они появятся здесь.", reply_markup=MAIN_KEYBOARD)
        return

    # «Где итог?» / «показать результат» — повторно отправить сохранённый анализ, если он есть
    ask_result = user_text.strip().lower()
    if ask_result in ("где итог?", "где итог", "где результат?", "где результат", "показать результат", "показать итог", "итог", "результат"):
        analysis = context.user_data.get("full_analysis", "")
        if analysis:
            to_show = analysis[:3997] + "..." if len(analysis) > 4000 else analysis
            formatted = _format_conclusion_for_elderly(to_show)
            await _send_long_html(context.bot, update.effective_chat.id, formatted, reply_markup=MAIN_KEYBOARD)
            continue_kb = InlineKeyboardMarkup([
                [InlineKeyboardButton("✅ Да, продолжить", callback_data=CB_CONTINUE_YES), InlineKeyboardButton("❌ Нет, спасибо", callback_data=CB_CONTINUE_NO)],
            ])
            await update.message.reply_text("Хотите продолжить?", reply_markup=continue_kb)
            return
        else:
            await update.message.reply_text(
                "Результаты анализа пока не готовы или сессия сброшена. Пройдите опрос заново или нажмите «Нет документов», когда бот попросит документы.",
                reply_markup=MAIN_KEYBOARD,
            )
            return

    # «Всё» / «готово» — показать выбор ИИ и ждать нажатия кнопки (или уже есть кнопки в предыдущем сообщении)
    if user_text.lower() in ("всё", "готово", "все", "готово."):
        if user_id in _pending and _pending[user_id]["file_ids"]:
            if context.job_queue:
                job_name = f"process_pending_{user_id}"
                for job in context.job_queue.jobs():
                    if job.name == job_name:
                        job.schedule_removal()
                        break
            task = _pending_tasks.pop(user_id, None)
            if task and not task.done():
                task.cancel()
            keyboard = _ai_choice_keyboard()
            if keyboard:
                await update.message.reply_text(
                    "Готово к анализу. Выберите ИИ:",
                    reply_markup=keyboard,
                )
            else:
                await _process_pending_images(context, user_id)
            return
        # иначе просто ответим, что нечего разбирать
        await update.message.reply_text("Пока нет документов для разбора. Пришли фото или файлы анализов/заключений.", reply_markup=MAIN_KEYBOARD)
        return

    # Умное определение намерения: collecting_docs -> AI-классификатор
    if context.user_data.get("collecting_docs"):
        classify_prompt = INTENT_CLASSIFY_PROMPT.format(user_text=user_text[:300])
        intent_raw = await _ask_ai_text(classify_prompt, user_text[:200])
        intent = (intent_raw or "").strip().lower().split()[0] if intent_raw else "other"
        if intent not in ("no_docs", "question", "other"):
            intent = "other"

        if intent == "no_docs":
            context.user_data["collecting_docs"] = False
            _pending.pop(user_id, None)
            patient_request = context.user_data.get("patient_request", "")
            survey_answers = context.user_data.get("completed_survey_answers") or {}
            survey_data = _format_survey_data(survey_answers)
            followup_answers = context.user_data.get("last_followup_answers", "") or ""
            followup_qa = ("\n\nОТВЕТЫ ПАЦИЕНТА НА УТОЧНЯЮЩИЕ ВОПРОСЫ:\n" + followup_answers.strip()[:2000]) if followup_answers.strip() else "Нет."
            documents_section = "Медицинские документы НЕ предоставлены. Используй только данные опроса, запрос и ответы на уточняющие вопросы."

            await update.message.reply_text("Провожу анализ на основе ваших данных…")

            prompt = FULL_ANALYSIS_PROMPT.format(
                survey_data=survey_data,
                patient_request=patient_request or "Не указан",
                followup_qa=followup_qa,
                documents_section=documents_section,
            )
            try:
                analysis = await _ask_ai_text(prompt, (patient_request or "анализ")[:500])
            except Exception:
                logger.exception("handle_text no_docs: AI error")
                await update.message.reply_text("Произошла ошибка при анализе. Попробуйте ещё раз через минуту.", reply_markup=MAIN_KEYBOARD)
                return
            if not analysis:
                await update.message.reply_text("Не удалось выполнить анализ. Попробуйте позже.", reply_markup=MAIN_KEYBOARD)
                return
            analysis = _strip_latex(analysis)
            analysis = _strip_foreign_chars(analysis)
            context.user_data["full_analysis"] = analysis
            _save_conclusion(user_id, analysis)
            # Сразу показываем выводы и рекомендации (частями, если длинно)
            to_show = analysis[:3997] + "..." if len(analysis) > 4000 else analysis
            formatted = _format_conclusion_for_elderly(to_show)
            await _send_long_html(context.bot, update.effective_chat.id, formatted, reply_markup=MAIN_KEYBOARD)
            continue_kb = InlineKeyboardMarkup([
                [InlineKeyboardButton("✅ Да, продолжить", callback_data=CB_CONTINUE_YES), InlineKeyboardButton("❌ Нет, спасибо", callback_data=CB_CONTINUE_NO)],
            ])
            await update.message.reply_text("Хотите продолжить?", reply_markup=continue_kb)
            return

        if intent == "question":
            patient_request = context.user_data.get("patient_request", "")
            survey_answers = context.user_data.get("completed_survey_answers") or {}
            survey_data = _format_survey_data(survey_answers)
            previous_analysis = context.user_data.get("full_analysis", "")

            q_prompt = FOLLOWUP_PROMPT.format(
                survey_data=survey_data,
                previous_analysis=previous_analysis[:3000] if previous_analysis else f"Описание ситуации: {patient_request}",
                followup_question=user_text,
                followup_qa="",
            )
            await update.message.reply_text("Отвечаю на ваш вопрос…")
            answer = await _ask_ai_text(q_prompt, user_text[:500])
            if answer:
                answer = _strip_latex(answer)
                answer = _strip_foreign_chars(answer)
            else:
                answer = "К сожалению, не удалось обработать вопрос. Попробуйте переформулировать."

            docs_kb = InlineKeyboardMarkup([
                [InlineKeyboardButton("📎 Отправить документы на анализ", callback_data=CB_SEND_DOCS)],
                [InlineKeyboardButton("📝 Нет документов", callback_data=CB_NO_DOCS)],
            ])
            await update.message.reply_text(answer, reply_markup=MAIN_KEYBOARD)
            await update.message.reply_text(
                "Вы также можете загрузить медицинские документы для более точного анализа "
                "или нажать «Нет документов», чтобы продолжить без них.",
                reply_markup=docs_kb,
            )
            return

        # intent == "other"
        docs_kb = InlineKeyboardMarkup([
            [InlineKeyboardButton("📎 Отправить документы на анализ", callback_data=CB_SEND_DOCS)],
            [InlineKeyboardButton("📝 Нет документов", callback_data=CB_NO_DOCS)],
        ])
        await update.message.reply_text(
            "Сейчас я ожидаю загрузку медицинских документов (фото или файлы).\n\n"
            "Если документов нет — нажмите кнопку ниже.",
            reply_markup=docs_kb,
        )
        return

    # Страховка: если пользователь ответил на уточняющий вопрос, но флаг по какой-то причине сброшен —
    # по наличию clarify_questions и короткому сообщению считаем это ответом и доводим сценарий до запроса документов
    questions = context.user_data.get("clarify_questions", [])
    step = context.user_data.get("clarify_step", -1)
    if questions and 0 <= step < len(questions) and len(user_text.strip()) <= 500:
        await _advance_clarify(update.effective_chat.id, context, user_text.strip())
        return

    has_groq = _use_groq()
    has_openai = bool(get_openai_client())
    if not has_groq and not has_openai:
        await update.message.reply_text(_no_ai_message())
        return

    await update.message.reply_text("Думаю…")

    text = ""
    last_err = None
    if has_groq:
        try:
            text = await _ask_groq_text(user_text)
        except Exception as e:
            last_err = e
            logger.warning("Groq при тексте: %s", e)
    if not text and has_openai:
        try:
            text = await _ask_openai_text(user_text)
        except Exception as e:
            last_err = e
            logger.warning("OpenAI при тексте: %s", e)

    if not text:
        msg = "Не удалось получить ответ. "
        if last_err:
            msg += _short_error(last_err)
        else:
            msg += "Проверь ключи в .env (Groq или OpenAI)."
        await update.message.reply_text(msg, reply_markup=MAIN_KEYBOARD)
        return
    if len(text) > 4000:
        text = text[:3997] + "..."
    await update.message.reply_text(text, reply_markup=MAIN_KEYBOARD)


async def _handle_patient_request(update: Update, context: ContextTypes.DEFAULT_TYPE, text: str) -> None:
    """Обработка запроса пациента: первичный или follow-up после анализа.
    Использует адаптивную систему вопросов — по одному за раз."""
    context.user_data["awaiting_request"] = False
    context.user_data["patient_request"] = text[:2000]

    survey_answers = context.user_data.get("completed_survey_answers") or {}
    survey_data = _format_survey_data(survey_answers)
    previous_analysis = context.user_data.get("full_analysis", "")
    patient_history = context.user_data.get("patient_history", [])
    history_text = _format_patient_history(patient_history)

    if previous_analysis:
        context.user_data["is_followup_request"] = True

    await update.message.reply_text("Анализирую ваш запрос…")

    first_q_prompt = ADAPTIVE_QUESTION_PROMPT.format(
        survey_data=survey_data,
        patient_history=history_text,
        patient_request=text[:1500],
        qa_so_far="Вопросы ещё не задавались.",
        question_number=1,
        max_questions=MAX_ADAPTIVE_QUESTIONS,
    )
    first_q_raw = await _ask_ai_text(first_q_prompt, text[:500])
    first_q = _parse_adaptive_question(first_q_raw) if first_q_raw else None

    if first_q:
        sa = context.user_data.get("completed_survey_answers") or {}
        patient_address = _get_patient_address(sa.get("q1", ""))
        name_part = f", <b>{_escape_html(patient_address)}</b>" if patient_address else ""

        context.user_data["clarify_questions"] = [first_q]
        context.user_data["clarify_answers"] = {}
        context.user_data["clarify_step"] = 0

        intro = (
            f"Для уточнения ситуации{name_part}, мне нужно задать вам "
            "несколько вопросов.\n\n"
            "Отвечайте так, как считаете нужным — это поможет дать точные рекомендации."
        )
        await update.message.reply_text(intro, parse_mode="HTML")
        await asyncio.sleep(3)
        await _send_clarify_question(update.effective_chat.id, context, 0)
        return

    if previous_analysis:
        context.user_data.pop("is_followup_request", None)
        prompt = FOLLOWUP_PROMPT.format(
            survey_data=survey_data,
            previous_analysis=previous_analysis[:3000],
            followup_question=text,
            followup_qa="",
            patient_history=history_text,
        )
        ai_response = await _ask_ai_text(prompt, text)
        if not ai_response:
            ai_response = "К сожалению, не удалось обработать запрос. Попробуйте переформулировать."

        ai_response = _strip_latex(ai_response)
        ai_response = _strip_foreign_chars(ai_response)

        context.user_data["collecting_docs"] = True
        user_id = update.effective_user.id
        _pending.pop(user_id, None)

        await update.message.reply_text(ai_response, reply_markup=MAIN_KEYBOARD)

        continue_kb = InlineKeyboardMarkup([
            [
                InlineKeyboardButton("✅ Продолжить", callback_data=CB_CONTINUE_YES),
                InlineKeyboardButton("🏁 Закончить", callback_data=CB_CONTINUE_NO),
            ],
        ])
        await update.message.reply_text(
            "Вы можете загрузить дополнительные документы для более точного анализа "
            "или задать ещё вопрос.\n\nХотите продолжить?",
            reply_markup=continue_kb,
        )
        return

    await _finish_patient_request_with_docs(update, context, survey_data, text, followup_answers="")


async def _send_clarify_question(chat_id: int, context: ContextTypes.DEFAULT_TYPE, step: int) -> None:
    """Выводит уточняющий вопрос номер step (0-based) с кнопками вариантов (если есть)."""
    questions = context.user_data.get("clarify_questions", [])
    if step >= len(questions):
        return
    q_data = questions[step]
    q_text = _strip_foreign_chars(q_data["q"] if isinstance(q_data, dict) else str(q_data))
    options = [_strip_foreign_chars(o) for o in (q_data.get("options", []) if isinstance(q_data, dict) else [])]
    yes_no_pattern = (
        r"\b(были|есть|принимали|принимаете|делали|делали ли|испытываете|имеется|было|обращались|обратились|"
        r"наблюдаете|наблюдали|изменялось|использовали)\s+ли\b"
        r"|\s+ли\s+(вы|у вас)\b"
    )
    if not options and re.search(yes_no_pattern, q_text, re.I) and not re.search(r"\bили\b", q_text, re.I):
        options = ["Да", "Нет"]

    context.user_data["clarify_step"] = step
    context.user_data["awaiting_clarify_answer"] = True

    header = f"<b>Вопрос {step + 1}</b>\n\n{_escape_html(q_text)}"
    if options:
        buttons = [[InlineKeyboardButton(opt, callback_data=f"{CB_CLARIFY_ANSWER}{i}")] for i, opt in enumerate(options)]
        kb = InlineKeyboardMarkup(buttons)
        await context.bot.send_message(chat_id, header, parse_mode="HTML", reply_markup=kb)
    else:
        await context.bot.send_message(
            chat_id,
            f"{header}\n\nНапишите ответ в чат.",
            parse_mode="HTML",
            reply_markup=MAIN_KEYBOARD,
        )


async def _advance_clarify(chat_id: int, context: ContextTypes.DEFAULT_TYPE, answer: str) -> None:
    """Адаптивный опрос: сохраняет ответ, генерирует следующий вопрос или завершает."""
    step = context.user_data.get("clarify_step", 0)
    answers = context.user_data.get("clarify_answers", {})
    answers[step] = answer
    context.user_data["clarify_answers"] = answers
    context.user_data["awaiting_clarify_answer"] = False

    questions = context.user_data.get("clarify_questions", [])
    next_number = step + 2  # step 0-based, question_number 1-based

    if next_number <= MAX_ADAPTIVE_QUESTIONS:
        survey_answers = context.user_data.get("completed_survey_answers") or {}
        survey_data = _format_survey_data(survey_answers)
        patient_request = context.user_data.get("patient_request", "")
        patient_history = context.user_data.get("patient_history", [])
        history_text = _format_patient_history(patient_history)
        qa_text = _format_qa_so_far(questions, answers)

        next_q_prompt = ADAPTIVE_QUESTION_PROMPT.format(
            survey_data=survey_data,
            patient_history=history_text,
            patient_request=patient_request[:1500],
            qa_so_far=qa_text,
            question_number=next_number,
            max_questions=MAX_ADAPTIVE_QUESTIONS,
        )
        next_q_raw = await _ask_ai_text(next_q_prompt, patient_request[:500])
        next_q = _parse_adaptive_question(next_q_raw) if next_q_raw else None

        if next_q:
            questions.append(next_q)
            context.user_data["clarify_questions"] = questions
            await _send_clarify_question(chat_id, context, step + 1)
            return

    qa_lines = []
    for i, q_data in enumerate(questions):
        q_text = q_data["q"] if isinstance(q_data, dict) else str(q_data)
        a_text = answers.get(i, "—")
        qa_lines.append(f"В: {q_text}\nО: {a_text}")
    followup_text = "\n\n".join(qa_lines)

    is_followup = context.user_data.pop("is_followup_request", False)

    context.user_data.pop("clarify_questions", None)
    context.user_data.pop("clarify_answers", None)
    context.user_data.pop("clarify_step", None)
    context.user_data.pop("awaiting_clarify_answer", None)

    patient_request = context.user_data.get("patient_request", "")
    survey_answers = context.user_data.get("completed_survey_answers") or {}
    survey_data = _format_survey_data(survey_answers)

    class _FakeMessage:
        def __init__(self, cid, b):
            self.chat_id = cid
            self._bot = b
        async def reply_text(self, text, **kw):
            return await self._bot.send_message(self.chat_id, text, **kw)

    class _FakeUpdate:
        def __init__(self, cid, b):
            self.effective_user = type("U", (), {"id": cid})()
            self.effective_chat = type("C", (), {"id": cid})()
            self.message = _FakeMessage(cid, b)

    fake_update = _FakeUpdate(chat_id, context.bot)

    if is_followup:
        previous_analysis = context.user_data.get("full_analysis", "")
        await _finish_followup_with_clarify(
            fake_update, context, survey_data, patient_request,
            previous_analysis, followup_text,
        )
    else:
        await _finish_patient_request_with_docs(fake_update, context, survey_data, patient_request, followup_answers=followup_text)


async def _finish_followup_with_clarify(
    update,
    context: ContextTypes.DEFAULT_TYPE,
    survey_data: str,
    patient_request: str,
    previous_analysis: str,
    followup_answers: str,
) -> None:
    """Follow-up анализ после уточняющих вопросов: использует FOLLOWUP_PROMPT с ответами."""
    followup_qa = ""
    if followup_answers and followup_answers.strip():
        followup_qa = followup_answers.strip()[:2000]

    prompt = FOLLOWUP_PROMPT.format(
        survey_data=survey_data,
        previous_analysis=previous_analysis[:3000],
        followup_question=patient_request,
        followup_qa=followup_qa,
    )
    await update.message.reply_text("Анализирую вашу ситуацию с учётом ответов…")
    ai_response = await _ask_ai_text(prompt, patient_request[:500])
    if not ai_response:
        ai_response = "К сожалению, не удалось обработать запрос. Попробуйте переформулировать."

    ai_response = _strip_latex(ai_response)
    ai_response = _strip_foreign_chars(ai_response)

    context.user_data["full_analysis"] = ai_response
    context.user_data["collecting_docs"] = True
    user_id = update.effective_user.id
    _pending.pop(user_id, None)

    await update.message.reply_text(ai_response, reply_markup=MAIN_KEYBOARD)

    continue_kb = InlineKeyboardMarkup([
        [
            InlineKeyboardButton("✅ Продолжить", callback_data=CB_CONTINUE_YES),
            InlineKeyboardButton("🏁 Закончить", callback_data=CB_CONTINUE_NO),
        ],
    ])
    await update.message.reply_text(
        "Вы можете загрузить дополнительные документы для более точного анализа "
        "или задать ещё вопрос.\n\nХотите продолжить?",
        reply_markup=continue_kb,
    )


async def _finish_patient_request_with_docs(
    update,
    context: ContextTypes.DEFAULT_TYPE,
    survey_data: str,
    patient_request: str,
    followup_answers: str,
) -> None:
    """После ответов на уточняющие вопросы (или без них): анализ запроса, список документов, кнопка загрузки."""
    followup_qa = ""
    if followup_answers and followup_answers.strip():
        followup_qa = "\n\nОТВЕТЫ ПАЦИЕНТА НА УТОЧНЯЮЩИЕ ВОПРОСЫ:\n" + followup_answers.strip()[:2000]

    prompt = REQUEST_ANALYSIS_PROMPT.format(
        survey_data=survey_data,
        patient_request=patient_request,
        followup_qa=followup_qa,
    )
    await update.message.reply_text("Анализирую ваш запрос…")
    ai_response = await _ask_ai_text(prompt, patient_request[:500])
    if not ai_response:
        ai_response = (
            "Пожалуйста, загрузите имеющиеся медицинские документы:\n\n"
            "📋 Анализы\n📋 Выписной эпикриз\n📋 Заключения врачей\n"
            "📋 Назначения и рецепты\n📋 Результаты обследований"
        )

    context.user_data["collecting_docs"] = True
    context.user_data["last_followup_answers"] = followup_answers or ""
    user_id = update.effective_user.id
    _pending.pop(user_id, None)

    keyboard = InlineKeyboardMarkup([
        [InlineKeyboardButton("📎 Отправить документы на анализ", callback_data=CB_SEND_DOCS)],
        [InlineKeyboardButton("📝 Нет документов", callback_data=CB_NO_DOCS)],
    ])
    await update.message.reply_text(
        f"{ai_response}\n\n"
        "Загрузите документы (фото или файлы). Когда всё прикрепите — нажмите кнопку ниже.\n"
        "Если документов нет — нажмите «Нет документов».",
        reply_markup=keyboard,
    )


async def handle_voice(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Голосовое сообщение: транскрибируем и обрабатываем как текст."""
    voice = update.message.voice
    if not voice:
        return
    try:
        tg_file = await context.bot.get_file(voice.file_id)
        buf = io.BytesIO()
        await tg_file.download_to_memory(buf)
        buf.seek(0)
        voice_bytes = buf.read()
    except Exception as e:
        logger.warning("Не удалось скачать голосовое: %s", e)
        await update.message.reply_text("Не удалось загрузить голосовое сообщение. Попробуйте ещё раз.")
        return

    await update.message.reply_text("Распознаю голос…")
    text = await asyncio.to_thread(_transcribe_voice_sync, voice_bytes)
    if not text:
        await update.message.reply_text(
            "Не удалось распознать речь. Попробуйте записать ещё раз или напишите текстом."
        )
        return
    await update.message.reply_text(f"Распознано: <i>{_escape_html(text[:500])}</i>", parse_mode="HTML")

    if context.user_data.get("awaiting_request"):
        await _handle_patient_request(update, context, text)
        return

    if context.user_data.get("awaiting_clarify_answer"):
        await _advance_clarify(update.effective_chat.id, context, text)
        return

    if context.user_data.get("awaiting_post_doc_answers"):
        context.user_data["awaiting_post_doc_answers"] = False
        post_doc_questions = context.user_data.pop("post_doc_followup_questions", [])
        full_analysis = context.user_data.get("full_analysis", "")
        if full_analysis:
            qa_block = "Ответы пациента на уточняющие вопросы:\n" + text[:2000]
            refine_prompt = REFINED_ANALYSIS_PROMPT.format(
                full_analysis=full_analysis[:6000],
                qa_block=qa_block,
            )
            await update.message.reply_text("Уточняю заключение с учётом ваших ответов…")
            refined = await _ask_ai_text(refine_prompt, qa_block)
            if refined:
                refined = _strip_latex(refined)
                context.user_data["full_analysis"] = refined
                _save_conclusion(update.effective_user.id, refined)
            keyboard = InlineKeyboardMarkup([
                [InlineKeyboardButton("📄 Показать результаты", callback_data=CB_SHOW_RESULTS)],
            ])
            await update.message.reply_text(
                "Готово. Нажмите кнопку ниже, чтобы увидеть итоговое заключение.",
                reply_markup=keyboard,
            )
        return

    # Страховка: ответ на уточняющий вопрос голосом при сброшенном флаге
    questions = context.user_data.get("clarify_questions", [])
    step = context.user_data.get("clarify_step", -1)
    if questions and 0 <= step < len(questions) and len(text.strip()) <= 500:
        await _advance_clarify(update.effective_chat.id, context, text.strip())
        return

    # Если не в режиме запроса — обрабатываем как обычный текстовый вопрос
    has_groq = _use_groq()
    has_openai = bool(get_openai_client())
    if not has_groq and not has_openai:
        await update.message.reply_text(_no_ai_message())
        return
    await update.message.reply_text("Думаю…")
    response = await _ask_ai_text(TEXT_PROMPT, text)
    if response:
        if len(response) > 4000:
            response = response[:3997] + "..."
        await update.message.reply_text(response, reply_markup=MAIN_KEYBOARD)
    else:
        await update.message.reply_text("Не удалось получить ответ.", reply_markup=MAIN_KEYBOARD)


def _transcribe_voice_sync(voice_bytes: bytes) -> str:
    """Синхронная обёртка для транскрибации (вызывается через asyncio.to_thread)."""
    buf = io.BytesIO(voice_bytes)
    buf.name = "voice.ogg"
    client = get_groq_client()
    if client:
        try:
            result = client.audio.transcriptions.create(model="whisper-large-v3", file=buf)
            return (result.text or "").strip()
        except Exception as e:
            logger.warning("Groq Whisper: %s", e)
    buf.seek(0)
    client = get_openai_client()
    if client:
        try:
            result = client.audio.transcriptions.create(model="whisper-1", file=buf)
            return (result.text or "").strip()
        except Exception as e:
            logger.warning("OpenAI Whisper: %s", e)
    return ""


async def handle_send_docs(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Кнопка 'Отправить документы на анализ' — полный анализ."""
    query = update.callback_query
    await query.answer()
    user_id = query.from_user.id if query.from_user else 0
    chat_id = query.message.chat_id
    bot = context.bot

    context.user_data["collecting_docs"] = False
    patient_request = context.user_data.get("patient_request", "")
    survey_answers = context.user_data.get("completed_survey_answers") or {}
    survey_data = _format_survey_data(survey_answers)

    data = _pending.pop(user_id, None)
    file_ids = data["file_ids"] if data else []

    if not file_ids:
        keyboard = InlineKeyboardMarkup([
            [InlineKeyboardButton("📎 Отправить документы на анализ", callback_data=CB_SEND_DOCS)],
            [InlineKeyboardButton("📝 Нет документов", callback_data=CB_NO_DOCS)],
        ])
        try:
            await query.edit_message_text(
                "Вы ещё не загрузили документы. Прикрепите фото или файлы и нажмите кнопку снова.\n"
                "Если документов нет — нажмите «Нет документов».",
                reply_markup=keyboard,
            )
        except Exception:
            pass
        if data:
            _pending[user_id] = data
        return

    try:
        await query.edit_message_text("Запускаю полный анализ…")
    except Exception:
        pass

    images_b64: List[tuple] = []
    for file_id, mime in file_ids:
        try:
            tg_file = await bot.get_file(file_id)
            buf = io.BytesIO()
            await tg_file.download_to_memory(buf)
            buf.seek(0)
            images_b64.append((base64.b64encode(buf.read()).decode("utf-8"), mime))
        except Exception as e:
            logger.warning("Не удалось загрузить файл %s: %s", file_id, e)

    if not images_b64:
        await bot.send_message(chat_id, "Не удалось загрузить документы. Попробуйте отправить снова.")
        return

    progress_msg = await bot.send_message(
        chat_id, f"Анализирую ваши данные…\n\n{_progress_bar(0)}"
    )
    stop_event = asyncio.Event()
    progress_task = asyncio.create_task(
        _progress_updater(bot, chat_id, progress_msg.message_id, stop_event)
    )

    followup_answers = context.user_data.get("last_followup_answers", "") or ""
    followup_qa = ("\n\nОТВЕТЫ ПАЦИЕНТА НА УТОЧНЯЮЩИЕ ВОПРОСЫ:\n" + followup_answers.strip()[:2000]) if followup_answers.strip() else "Нет."
    documents_section = "К сообщению приложены медицинские документы пациента (изображения). Внимательно изучи ВСЕ документы и используй данные из них в итоге."
    prompt = FULL_ANALYSIS_PROMPT.format(
        survey_data=survey_data,
        patient_request=patient_request or "Не указан",
        followup_qa=followup_qa,
        documents_section=documents_section,
    )
    user_msg = (
        f"Запрос пациента: {patient_request}\n\n"
        "Приложены медицинские документы. Проведи полный анализ по инструкции (8 пунктов итога)."
    )
    try:
        analysis = await _ask_ai_with_images(prompt, user_msg, images_b64)
    finally:
        stop_event.set()
        progress_task.cancel()
        try:
            await progress_task
        except asyncio.CancelledError:
            pass
        try:
            await bot.edit_message_text(
                chat_id=chat_id,
                message_id=progress_msg.message_id,
                text=f"Анализирую ваши данные…\n\n{_progress_bar(100)}",
            )
        except Exception:
            pass
        await asyncio.sleep(0.3)
        try:
            await bot.delete_message(chat_id=chat_id, message_id=progress_msg.message_id)
        except Exception:
            pass

    if not analysis:
        await bot.send_message(
            chat_id,
            "Не удалось провести анализ. Попробуйте ещё раз или загрузите документы в другом формате.",
            reply_markup=MAIN_KEYBOARD,
        )
        return

    context.user_data["full_analysis"] = analysis
    _save_conclusion(user_id, analysis)

    # Уточняющие вопросы после анализа документов для более точного заключения
    analysis_summary = (analysis[:1500] + "…") if len(analysis) > 1500 else analysis
    post_doc_prompt = POST_DOC_QUESTIONS_PROMPT.format(
        patient_request=patient_request or "Не указан",
        analysis_summary=analysis_summary,
    )
    questions_raw = await _ask_ai_text(post_doc_prompt, analysis_summary[:500])
    post_doc_questions = _parse_questions_from_ai(questions_raw) if questions_raw else []

    if post_doc_questions:
        context.user_data["post_doc_followup_questions"] = post_doc_questions
        context.user_data["awaiting_post_doc_answers"] = True
        questions_text = "\n".join(f"• {q['q'] if isinstance(q, dict) else q}" for q in post_doc_questions)
        await bot.send_message(
            chat_id,
            "<b>✅ АНАЛИЗ ВЫПОЛНЕН</b>\n\n"
            "Чтобы уточнить заключение и рекомендации, ответьте, пожалуйста, на несколько вопросов:\n\n"
            f"{questions_text}\n\n"
            "Напишите ответы текстом или голосовым сообщением (можно одним сообщением).",
            parse_mode="HTML",
            reply_markup=MAIN_KEYBOARD,
        )
    else:
        keyboard = InlineKeyboardMarkup([
            [InlineKeyboardButton("📄 Показать результаты", callback_data=CB_SHOW_RESULTS)],
        ])
        await bot.send_message(
            chat_id,
            "<b>✅ АНАЛИЗ ВЫПОЛНЕН</b>\n\nЯ готов вывести результаты на экран.",
            parse_mode="HTML",
            reply_markup=keyboard,
        )


async def handle_no_docs(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Кнопка 'Нет документов' — анализ только на основе опроса и запроса."""
    query = update.callback_query
    await query.answer()
    user_id = query.from_user.id if query.from_user else 0
    chat_id = query.message.chat_id
    bot = context.bot

    context.user_data["collecting_docs"] = False
    _pending.pop(user_id, None)

    patient_request = context.user_data.get("patient_request", "")
    survey_answers = context.user_data.get("completed_survey_answers") or {}
    survey_data = _format_survey_data(survey_answers)
    followup_answers = context.user_data.get("last_followup_answers", "") or ""
    followup_qa = ("\n\nОТВЕТЫ ПАЦИЕНТА НА УТОЧНЯЮЩИЕ ВОПРОСЫ:\n" + followup_answers.strip()[:2000]) if followup_answers.strip() else "Нет."
    documents_section = "Медицинские документы НЕ предоставлены. Используй только данные опроса, запрос и ответы на уточняющие вопросы."

    try:
        await query.edit_message_text("Провожу анализ на основе ваших данных…")
    except Exception:
        pass

    prompt = FULL_ANALYSIS_PROMPT.format(
        survey_data=survey_data,
        patient_request=patient_request or "Не указан",
        followup_qa=followup_qa,
        documents_section=documents_section,
    )

    try:
        analysis = await _ask_ai_text(prompt, (patient_request or "анализ")[:500])
    except Exception:
        logger.exception("handle_no_docs: AI error")
        await bot.send_message(chat_id, "Произошла ошибка при анализе. Попробуйте ещё раз через минуту.", reply_markup=MAIN_KEYBOARD)
        return
    if not analysis:
        await bot.send_message(chat_id, "Не удалось выполнить анализ. Попробуйте позже.", reply_markup=MAIN_KEYBOARD)
        return

    analysis = _strip_latex(analysis)
    analysis = _strip_foreign_chars(analysis)
    context.user_data["full_analysis"] = analysis
    _save_conclusion(user_id, analysis)

    # Сразу показываем выводы и рекомендации (без кнопки «Показать результаты»)
    to_show = analysis[:3997] + "..." if len(analysis) > 4000 else analysis
    formatted = _format_conclusion_for_elderly(to_show)
    logger.info("handle_no_docs: отправляю выводы пользователю chat_id=%s", chat_id)
    await _send_long_html(bot, chat_id, formatted, reply_markup=MAIN_KEYBOARD)
    continue_kb = InlineKeyboardMarkup([
        [InlineKeyboardButton("✅ Да, продолжить", callback_data=CB_CONTINUE_YES), InlineKeyboardButton("❌ Нет, спасибо", callback_data=CB_CONTINUE_NO)],
    ])
    await bot.send_message(chat_id, "Хотите продолжить?", reply_markup=continue_kb)


async def handle_show_results(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Кнопка 'Показать результаты' — вывод полного анализа."""
    query = update.callback_query
    await query.answer()
    chat_id = query.message.chat_id
    bot = context.bot

    analysis = context.user_data.get("full_analysis", "")
    if not analysis:
        await bot.send_message(chat_id, "Результаты анализа не найдены.", reply_markup=MAIN_KEYBOARD)
        return

    try:
        await query.message.delete()
    except Exception:
        pass

    if len(analysis) > 4000:
        analysis = analysis[:3997] + "..."
    formatted = _format_conclusion_for_elderly(analysis)
    await bot.send_message(
        chat_id,
        formatted,
        parse_mode="HTML",
        reply_markup=MAIN_KEYBOARD,
    )

    continue_kb = InlineKeyboardMarkup([
        [
            InlineKeyboardButton("✅ Да, продолжить", callback_data=CB_CONTINUE_YES),
            InlineKeyboardButton("❌ Нет, спасибо", callback_data=CB_CONTINUE_NO),
        ],
    ])
    await bot.send_message(
        chat_id,
        "Хотите продолжить?",
        reply_markup=continue_kb,
    )


async def handle_continue(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Обработка кнопок 'Да, продолжить' / 'Нет, спасибо' после анализа."""
    query = update.callback_query
    await query.answer()
    data = (query.data or "").strip()
    chat_id = query.message.chat_id
    bot = context.bot

    try:
        await query.message.delete()
    except Exception:
        pass

    if data == CB_CONTINUE_YES:
        context.user_data["collecting_docs"] = True
        context.user_data["awaiting_request"] = True
        await bot.send_message(
            chat_id,
            "Отлично! Вы можете:\n\n"
            "  — задать вопрос текстом или голосовым сообщением\n"
            "  — загрузить дополнительные документы или фото\n\n"
            "Я отвечу на ваш вопрос и учту новые данные в анализе.",
            reply_markup=MAIN_KEYBOARD,
        )
    elif data == CB_CONTINUE_NO:
        context.user_data.pop("full_analysis", None)
        context.user_data.pop("collecting_docs", None)
        context.user_data.pop("awaiting_request", None)
        context.user_data.pop("completed_survey_answers", None)
        context.user_data.pop("patient_request_text", None)
        await bot.send_message(
            chat_id,
            "Благодарю вас за доверие! Берегите себя и будьте здоровы. 🙏\n\n"
            "Если понадобится помощь — я всегда рядом. "
            "Просто напишите /start, и мы начнём консультацию в любое удобное для вас время.",
            reply_markup=MAIN_KEYBOARD,
        )


async def handle_clarify_callback(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Кнопка с вариантом ответа на уточняющий вопрос."""
    query = update.callback_query
    await query.answer()
    data = (query.data or "").replace(CB_CLARIFY_ANSWER, "", 1)
    try:
        opt_idx = int(data)
    except ValueError:
        return
    questions = context.user_data.get("clarify_questions", [])
    step = context.user_data.get("clarify_step", 0)
    if step >= len(questions):
        return
    q_data = questions[step]
    options = q_data.get("options", []) if isinstance(q_data, dict) else []
    if opt_idx < 0 or opt_idx >= len(options):
        return
    answer = options[opt_idx]
    try:
        await query.edit_message_text(
            f"<b>Вопрос {step + 1} из {context.user_data.get('clarify_total', len(questions))}</b>\n\n"
            f"{_escape_html(q_data['q'] if isinstance(q_data, dict) else str(q_data))}\n\n"
            f"✅ <i>{_escape_html(answer)}</i>",
            parse_mode="HTML",
        )
    except Exception:
        pass
    await _advance_clarify(query.message.chat_id, context, answer)


async def handle_contact(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Пользователь поделился контактом — сохраняем телефон в таблицу."""
    contact = update.message.contact
    if not contact or not contact.phone_number:
        return
    phone = contact.phone_number
    context.user_data["tg_phone"] = phone
    sheet_row = context.user_data.get("survey_sheet_row")
    if sheet_row:
        await asyncio.to_thread(
            lambda: _sheet_update_phone(sheet_row, phone)
        )
    await update.message.reply_text(
        f"Спасибо! Номер {phone} сохранён.",
        reply_markup=MAIN_KEYBOARD,
    )


def _sheet_update_phone(row_index: int, phone: str) -> Optional[str]:
    """Записывает номер телефона в столбец D (телефон)."""
    try:
        wks, err = _get_sheet_wks()
        if wks is None:
            return err
        wks.update_cell(row_index, 4, phone)
        return None
    except Exception as e:
        _sheet_cache["wks"] = None
        return str(e)[:200]


def main() -> None:
    token = os.getenv("BOT_TOKEN")
    if not token:
        raise ValueError("Задай BOT_TOKEN в .env или в переменных окружения")

    app = Application.builder().token(token).build()

    app.add_handler(CommandHandler("start", start))
    app.add_handler(CommandHandler("help", help_command))
    app.add_handler(CallbackQueryHandler(handle_auth_choice, pattern="^auth:"))
    app.add_handler(CallbackQueryHandler(handle_flow_start, pattern="^flow:"))
    app.add_handler(CallbackQueryHandler(handle_consent, pattern="^consent:"))
    app.add_handler(CallbackQueryHandler(handle_ai_choice, pattern="^ai:"))
    app.add_handler(CallbackQueryHandler(handle_next_step, pattern="^next:"))
    app.add_handler(CallbackQueryHandler(handle_survey_callback, pattern="^survey:"))
    app.add_handler(CallbackQueryHandler(handle_no_docs, pattern="^docs:none$"))
    app.add_handler(CallbackQueryHandler(handle_send_docs, pattern="^docs:send$"))
    app.add_handler(CallbackQueryHandler(handle_show_results, pattern="^results:"))
    app.add_handler(CallbackQueryHandler(handle_continue, pattern="^continue:"))
    app.add_handler(CallbackQueryHandler(handle_clarify_callback, pattern="^clarify:"))
    app.add_handler(MessageHandler(filters.CONTACT, handle_contact))
    app.add_handler(MessageHandler(filters.VOICE, handle_voice))
    app.add_handler(MessageHandler(filters.PHOTO, handle_photo))
    app.add_handler(MessageHandler(filters.Document.ALL, handle_document))
    app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_text))

    logger.info("Бот запущен (опросник: %d вопросов; анализ без доков — выводы сразу)", len(MEDICAL_QUESTIONS))
    app.run_polling(allowed_updates=Update.ALL_TYPES, drop_pending_updates=True)


if __name__ == "__main__":
    main()
